================================================================================
  ビジネスダイアグラムメーカー 開発記録 v8（why／経緯）
================================================================================

  本ファイルは v8 系の「なぜ・経緯・原因と対策・既知の注意」を記録する。
  各版で「何を変えたか（what）」は git のコミットメッセージを参照。
  索引・運用は 開発記録.txt／運用ルール.txt、計画は 開発ロードマップ.txt。

  ──────────────────────────────────────────────────────────────────────────────
  v8.01（2026-07-01）複数選択・水平垂直ドラッグ・レーン色・デザインテンプレート・おしゃれステップ図
  ──────────────────────────────────────────────────────────────────────────────

  背景・目的:
    ①選択とドラッグの操作性を一般的な作図ツール（PowerPoint 等）に合わせたい、②レーン色を
    後から手軽に変えたい、③図の「色は変えずに見た目（角丸・線・影）だけ」を一括で整えたい、
    ④画像で示された番号サークル/矢羽/ピルのようなインフォグラフィック調のステップ図を作りたい、
    というユーザー要望。①〜③は明確なので即実装、④は見た目主体のため事前にHTML/SVGモックで
    複数案を提示し、採用テンプレを決めてから実装した。

  設計上の判断（落とし穴と対策）:
    ・修飾キーの役割を「押した瞬間」に確定させない。従来は Ctrl+mousedown で即複製していたため、
      Ctrl+クリック＝複数選択と両立できなかった。対策＝mousedown では通常ドラッグを仕込むだけにし、
      「クリック確定(mouseup・未移動)＝選択トグル」「ドラッグ確定(mousemove・4px超)＝Ctrlでコピー化
      (convertDragToCopy)」に振り分けた。convertDragToCopy を必ず S_didDrag 成立後に呼ぶ点が肝
      （指の微揺れで誤コピーしないため）。Ctrl/Shift どちらのクリックでも複数選択できる。
    ・Shift+ドラッグの水平/垂直固定は、mousemove 冒頭で pt を制約（変位の大きい軸だけ残す）すると
      下流の全分岐（group / 自由配置 / レーン）に一括で効く。ただし整列・グリッド・レーン内吸着が
      固定軸を動かしてしまうので、S.drag.constrained のとき該当軸のスナップを無効化して軸を純粋に
      固定する。
    ・デザインテンプレート（S.style / STYLES）は「色（各シェイプの fill/stroke・レーンの bg/hd）は
      一切変えず、形の装飾（角丸rx倍率・線幅・影フィルタ・アクセント帯・矢羽/ピル形状）だけ」を
      差し替える方針。shapeEl の rx を styRx()、線幅を stySW() 経由にし、影とアクセント帯は
      renderNodes で shapeEl の戻り要素へ後付け（テキストに影を掛けない）。矢羽/ピルは基本の
      「プロセス(rect)」の形のみ差し替え（他 rect 系は角丸だけ変化）＝ガント等を壊さないため。
      影は SVG フィルタ（ds-*）を defs に静的定義。保存/復元に S.style・S.stepNums を追加。
    ・おしゃれステップ図: 番号サークルは新シェイプ step_num（円＋大きな01/02/03＋下にラベル）。
      番号は「読み順（横広がりなら左→右／縦広がりなら上→下）」で毎描画時に自動採番（stepOrder）
      するため、並べ替え・追加で番号が自動で振り直る。番号バッジ(S.stepNums)は一般のプロセス系
      シェイプ左上にも同ロジックで付与。配色テーマは作成済みシェイプへ色を上書きする明示操作
      （Ctrl+Z で戻せる）＝既定方針「色は変えない」の任意的な例外として提供。レイアウト挿入は
      既存の addNode/addEdge 相当で編集可能なノード列＋接続線を生成（挿入後もドラッグ/文字編集可）。

  既知の注意:
    ・デザインテンプレートと配色テーマは図全体（全図種）に効く（S.style はグローバル）。矢羽テンプレは
      「プロセス(rect)」のみ形が変わる。step_num は自由配置シェイプ扱い（レーンに拘束されない）。

  追補（v8.01 同セッション・版番据置）:
    ・レイアウト挿入を縦横モード追従に変更。挿入時 isV() を見て、縦モード＝上→下の列／横モード＝
      左→右の行で並べる。挿入後に向きを切り替えても toggleOrientation の全ノード x↔y 入替で列ごと
      回転する（番号は stepOrder が読み順を再判定して追従、番号サークルのラベルは縦並び＝円の右／
      横並び＝円の下へ _flowDirs で自動切替）。
    ・ステップ数を可変に。挿入した列を flowId で1グループとして管理し、列末尾に＋（flowAddStep＝末尾へ
      1つ追加・並び方向を継続）／－（flowRemoveStep＝末尾を1つ削除・最低1つは残す）ボタンを描画
      （renderFlowControls）。ボタンは選択モード時のみ表示し、class="flow-ctrl" を付けて
      buildExportSvg のクローンから除去＝SVG/PNG/PDF には出さない。


  背景・目的:
    建設・土木の施工工程表（バーチャート式）は、用紙上部に 工事名・工事場所・発注者・
    施工者・工期・担当（現場代理人等）・作成日・版数 を載せ、右上に承認（作成・確認・承認の
    押印）欄、下部に凡例を置くのが定番。これまでガントは時間軸ヘッダー(2段)しか無く実務の
    工程表として提出しづらかった。Web調査で項目名・配置の定番を確定した上で実装。現場ごとに
    記載項目が異なるため「自由な行（ラベル：値）の追加・編集・並べ替え」でカスタマイズ可能に。

  設計上の最重要判断（落とし穴と対策）:
    画面の当たり判定は全箇所 (clientY - rect.top)/zoomLevel のピクセル換算で、SVGの viewBox
    オフセットを考慮していない（例: クリック無視ガード if(py<GANTT_HEADER_H)、ganttSlotIndexAtY）。
    そのため「エクスポートで既に使っている負Y方向 viewBox 帯」を画面にも使うとクリック位置が
    全部ズレる。
    → 採用方式: 見出しを “上部の非操作ストリップ（正Y空間 y:0..ganttHeadH()）” として描き、
      時間軸ヘッダー・グリッド・行を見出し高さぶん下げる。下げ量を ganttBodyTop()
      （= ganttHeadH() + GANTT_HEADER_H）に集約し、「内容の上端＝GANTT_HEADER_H」だった参照を
      これに一括置換。時間軸ヘッダー(renderGanttHeader)は内部座標を一切変えず translate グループで
      平行移動（DOMイベント＋svgPt はそのまま効くので＋ボタン・列幅リサイズも不変）。
      これらをライブSVGへ描くので buildExportSvg のクローンにそのまま乗り、SVG/PDF出力が自動追従。
      画面の座標系は従来どおり「ユーザー単位＝ピクセル・原点0」を維持＝当たり判定は上端定数の
      差し替えのみで済む。

  レイアウト確定（モック反復でユーザーと合意）:
    工事名（大）＋情報を左に2列・下詰め（本体との余白を詰める）／右上に作成日＋承認欄（小さめ・
    画面の並びは左→右で承認・確認・作成＝紙面では右から作成・確認・承認）／凡例は「押印欄の下・
    右側ゾーン内」で横長（3列×2段）・角丸の黒枠で囲む（説明欄の下には侵入させない）。
    見出しの高さは行数・承認ON/OFF・凡例項目数で自動可変（ganttHeadModel が縦寸法のみ算出、
    横位置は描画時に SVG_W から右端基準で決定）。

  データモデル / 永続化 / 後方互換:
    S.gantt.header = {show,title,rows:[{label,value}],showDate,approval:{show,slots:[承認,確認,作成]}}
    S.gantt.legend = {show,auto,overrides:{key→ラベル上書き},extra:[{label,color,stroke}]}
    approval.slots は「描画順＝左→右」で格納（紙面で右から作成のため、表示時に右寄せ配置）。
    保存は saveHtmlCore が S.gantt 全体を JSON 化するので追加フィールドも自動で永続化される。
    旧ファイル（header/legend 無し）は描画前に ganttEnsureHeader() が既定補完＝そのまま開ける。

  凡例の自動生成:
    ganttLegendItems() が S.nodes を走査し、使われている要素だけを拾う＝タスク（予定）／進捗
    （progress>0 のとき）／マイルストーン／フェーズ／休工（suspensions 有り）／土日。色見本は
    SD.gantt_* の fill/stroke を参照（図と完全一致）。overrides でラベルだけ差し替え、extra で
    「電気設備（協力会社）」等の工種・業者色分けを手動追加できる。

  エクスポート反映:
    ・SVG/PNG: ライブSVGに描くのでクローンに自動で乗る。見出し表示時は buildExportSvg の図面名
      タイトル帯を省略（工事名と二重化しないため）。
    ・PDF: 表題＋時間軸ヘッダーを <thead> に内包し各ページ先頭で繰り返す（viewBox を 0..ganttBodyTop()
      に拡張）。本体はフェーズ境界で改ページ（従来どおり）。図面名帯は見出し表示時は出さない。
    ・Excel: 帯(BAND)を「1 + 見出し情報行数」に拡張し、行1に工事名＋作成日、続く行に情報2列、
      （承認ONなら）承認欄をテキストで展開。最下部に凡例（色塗りセル＋ラベル）を1行追加。
      freeze/printTitlesRow は時間軸のみ繰り返す既存挙動を踏襲（情報帯は1ページ目先頭のみ）。

  既知の注意:
    ・見出し表示時は calcCanvasSize で SVG_W を最低 1120px 確保（狭い図でも右ゾーン＝承認欄＋凡例が
      重ならないように）。
    ・設定UIの動的リスト（行・承認・凡例）はテキスト入力にフォーカス中は作り直さない（ganttHeadEditing
      ガード）＝入力中のキャレット/フォーカスを維持しつつ SVG だけライブ更新。

  検証（Claude_Preview 実DOM ＋ node --check）:
    headH＝承認OFF:164／ON:176、行数で追従。bodyTop=232 でクリック→正しいタスクindex、
    header OFF で bodyTop=56（＝完全に従来挙動・回帰なし）。設定UI＝行の追加/削除/▲▼並べ替え・
    承認トグル・凡例ラベル上書き・工種色分け追加が即反映。保存→JSON→復元ラウンドトリップで
    header/legend 保持。旧形式（header/legend 無し）も補完して描画。buildExportSvg は工事名を内包し
    図面名帯の二重化なし（nameBand=0）。PDF/Excel いずれも例外0で生成（Excel 13.8KB 出力確認）。
    全インラインスクリプトの node --check ＝SYNTAX_OK。

  （diagram_v7.11.html をコピーして diagram_v8.00.html を新規作成。<title>／#app-title-bar／serve.js 既定
    配信先を v8.00 に更新。大変更＝整数部を v8 に。次の小変更は v8.01。）

  ──────────────────────────────────────────────────────────────────────────────
  v8.00 追補（2026-06-30）情報欄（概要列）の操作性改善＝列削除・項目名の表示/編集まわり
  ──────────────────────────────────────────────────────────────────────────────
    版番据置の追補（バグ修正＋微改善）。what はコミット参照。why／原因と対策のみ記す。

    ① 追加列を削除する手段が無かった → 列ヘッダー下端中央にホバー表示の削除アイコン（赤×）を
       追加。確認ダイアログ（showConfirm）で警告してから削除し、列構成と各タスクの cells を破棄。
       既定列 c0（概要/担当＝desc/assignee bind）は基幹のため対象外（誤渡しもガード）。
       アイコン位置はユーザー指示で「ヘッダー行の下端」に統一（＋列追加ボタンも下端へ）。

    ② バグ: 追加列のセルは「本文(値)が非空のときだけ描画」していたため、項目名(ラベル)だけ
       入力しても何も表示されなかった（ユーザー指摘＝編集しても出てこない）。
       → 描画条件を「本文が非空、または 追加列で項目名が非空」に変更。c0 は従来どおり本文が
         空なら既定ラベル(概要/担当)も非表示＝空タスクで「概要：/担当：」を並べない挙動を維持。

    ③ 項目名を「列単位（全タスク共通）」に変更（ユーザー選択＝B案）。従来は項目名をタスクごとに
       cell.l で保持していたため、列見出しとして不自然だった。col.r3Label/col.r4Label に集約し、
       ganttColLabel/ganttSetColLabel を新設、ganttCellLabel(n,…) は列ラベルを返す薄いラッパに。
       本文(値)は引き続きタスクごと（c0=desc/assignee, 追加列=cells[colId].v）。
       後方互換: 旧データ（タスク別 cell.l）は ganttMigrateColLabels() が列ラベルへ集約（最初の
       非空を採用・冪等）。読込・インポート・undo復元の各経路に挿入し既存ファイルの項目名を保全。

    検証（Claude_Preview 実DOM）: 削除＝ホバー表示/確認後に列とcells破棄・c0は非対象。表示＝
    項目名のみでも追加列は表示・c0空はラベル非表示。列単位＝1タスクの項目名変更が全タスクへ反映、
    本文はタスク別、旧データ移行で「工区」を列へ集約、undo/redoで列ラベル復元。コンソールエラー0。

================================================================================
【追補】v8.00（版番据置・バグ修正） 2026-06-29
--------------------------------------------------------------------------------
■ 左サイドバー（編集バー）にスクロールを追加
  why: 見出し・凡例パネルが縦に長く（工事場所等の情報行＋承認欄＋凡例）、画面が低いと
       下側のコントロール（承認欄・凡例設定など）が画面外に隠れて操作不能になっていた。
  how: #app-header（図の名称欄）は固定のまま、その下の全セクション（シェイプ〜レーン）を
       #sidebar-scroll でくくり、flex:1 + min-height:0 + overflow-y:auto で縦スクロール化。
       ヘッダーは常時見える＝図の名称をいつでも編集できる。

■ 見出しの「工事名」を図の名称に一本化（見出し専用の工事名入力を廃止）
  why: 「工事名」と「図の名称」を二重入力させていた。実質同じものなので二度手間・不整合の元。
  how: 見出しパネルの工事名 input（gh-title）を撤去。renderGanttTitleBlock と Excel 出力の
       工事名は exportDiagramName()（＝S_diagramName＝上部「図の名称」）を参照。図の名称欄の
       oninput を onDiagramNameInput() に変更し、ガント＋見出し表示中は即 render() で
       タイトルをライブ追従。h.title は不参照（旧保存データの title は無視・後方互換で実害なし）。
  検証: Claude_Preview 実DOMで、図の名称編集→見出しタイトルが即時更新／工事名入力欄が消滅／
        サイドバーが scrollHeight>clientHeight でスクロール可／console エラー0 を確認。
================================================================================

================================================================================
【追補】v8.00（版番据置・改善） 2026-06-30
--------------------------------------------------------------------------------
■ 見出し（工事名・工事場所など）の編集を「ダブルクリックで開くダイアログ」に変更
  why: 見出しの編集欄を左サイドバー（幅250px）に置いていたため、1行が「ラベル＋値＋×」で
       横に詰まり、長い住所・会社名が見切れて入力しづらかった（ユーザー指摘＝狭すぎる）。
       タスク詳細編集と同じ「対象をダブルクリック→ダイアログ」の作法に揃える方が一貫する。
  方式（ユーザー選択＝B案・モック合意済み）: 見出し領域を覆う透明ヒット矩形を
       renderGanttTitleBlock に追加し、ダブルクリックで #gantt-head-modal（幅560px）を開く。
       サイドバーからは入力リスト（情報行/承認スロット/凡例上書き）を撤去し、表示ON/OFF等の
       トグルと「見出しを編集…」ボタンだけ残す（ユーザー指示＝トグルのみ）。
  設計: データモデル（header.rows / approval.slots / legend）は不変＝PDF/Excel/SVG出力は無改修で
       追従。ダイアログは既存setter（ganttHeadSetRow等）を再利用しライブプレビュー。開いた時点の
       header/legend/図名称を _ghBackup に退避し、OK=snap()確定／閉じる(=Esc/背景)=破棄して復元。
       動的リストの作り直しはテキスト入力フォーカス中スキップ（ganttHeadDlgEditing）でキャレット保持。
       工事名はダイアログ内でも「図の名称」と双方向連動（ganttHeadDlgSetTitle）。
  検証: Claude_Preview 実DOMで、ダブルクリック/サイドバーボタンの両方で起動・工事名/行/承認/凡例の
       編集がSVGへ即反映・行の追加/削除/▲▼・承認/凡例トグルでスロット/上書き欄が増減・OK確定で
       snap・閉じるで全項目バックアップ復元・サイドバーはトグル5個＋編集ボタンのみ・console エラー0。
================================================================================

================================================================================
【v8.02】スイムレーン等の接続線を改良（ラベル線上化・○ホバー化・終点整形・段違い） 2026-07-01
--------------------------------------------------------------------------------
■ 発端（ユーザー指摘・4点）
  ① 接続線のラベル/編集欄が線上に出ない（端点の直線中点＝折れ線から浮く）。
  ② ラベル無し線の○（追加ボタン）が常時表示でうるさい。
  ③ 1つの相手に加えて2つ目へ繋ぐと、扇状分散で1本目の始点がずれてL字に曲がる（真下なのに斜行）。
  ④ 3つ目へ繋ぐと2本目と3本目の横線が同じ高さで重なる。

■ 仕様決定（AskUserQuestion で合意・モックで反復）
  ・始点(出口)は等間隔(扇状分散)のまま許容。終点は「一直線にできる線は一直線を優先(端が中央で
    なくても可)／できない線は辺の中央に着地」。相手シェイプごとに判定を変える。
  ・重なり回避は「曲がる高さ(横トラック)を枝ごとに段違い」。ただし素直に段違いにすると外側の枝が
    内側の枝の横線を突っ切る＝クロスが出る（ユーザーが不可）。
  ・角丸など線の描き方は現行維持（今回いじるのは経路の座標＝frac と段違い位置だけ）。

■ クロスを出さない段違いの原理（重要・why）
  出口辺を共有する“曲がる線”を、相手までの距離が【近い相手＝相手寄り(下段)／遠い相手＝始点寄り
  (上段)】のトラックに置く。こうすると各線の縦の降り口は自分のトラックまでしか下りず、遠い相手の
  長い横線は近い相手の短い横線より上を通り、互いの縦線が相手の横線を突っ切らない＝交差も重なりも
  出ない（源が箱幅より広く相手が横に散る配置でも成立）。逆順(遠い=下段)にすると必ず交差する。

■ 実装（diagram_v8.02.html）
  ・allocatePorts 末尾に段違いパスを追加（['swimlane','flowchart','bpmn'] のみ対象）。
    出口辺ごとにグループ化→各線の出口X/Yを算出→終点 entryFrac を直線化(範囲内なら exitに一致)／
    中央(0.5)に決定→曲がる線が2本以上なら距離順に近い→遠いで段違いトラック位置 autoMid を割当。
    相手に複数入線する辺・始点/終点ピン・ひし形・戻り線・同一辺Uターンは対象外(従来動作を保持)。
  ・renderEdges: 手動 midPos が無い線は autoMid を orthoRoute へ渡す(_amid)。ラベル/○の基準点
    mx,my を端点中点から polylineMid(wps)＝実際の折れ線の中央へ変更。○は未選択時 opacity:0 とし
    線(ヒットパス)/○自身の mouseenter で表示・mouseleave で非表示(選択中は常時表示)。
  ・orthoRoute/角丸(wpPath,18)は不変＝終点・段違いは frac と midPos の指定だけで実現。

■ 検証
  puppeteer で 1→3(3レーン)を構築し実DOMのパスdを確認：1本目=真下へ一直線、2本目=下段トラックで
  相手中央着地、3本目=上段トラックで相手中央着地、交差・重なり無し、角は Q(角丸)維持。○は未選択線
  opacity=0／選択線 opacity=1、ラベルは polylineMid で線上。console エラー0。
================================================================================

--------------------------------------------------------------------------------
【v8.02 追補】遠い相手への接続を「上辺入り」に揃える（始点=右辺は従来のまま） 2026-07-01
--------------------------------------------------------------------------------
■ 発端（ユーザー指摘）
  横に大きく離れた相手への線が、相手の「左辺」から入り、途中の箱の高さを走って貫通しうる。
  原因は入口辺 entrySide=facingSide(相手,始点) が、始点が「上」より「横」に離れていると“横向き”と
  判定して左右辺入りにするため（縦成分 |dy| >= |dx|*0.27 のしきい値）。
■ 決定（ユーザー指示）: 始点(出口)は従来通り右辺から出してよい。終点(入口)だけ上辺に揃える。
■ 実装（diagram_v8.02.html・allocatePorts）
  ・1st pass: scoped(swimlane/flowchart/bpmn)・非ピン・非ひし形・非戻り線で、相手が主軸方向に完全に
    離れている(縦モード=相手が始点の下辺より下/上辺より上、横モード=右/左)線は entrySide を主軸の辺
    (上/下辺、横モードは左/右辺)へ上書き。exitSide は facingSide のまま（右出しを維持）。
  ・段違いパス: 出口と入口の軸が「直交」する群（例 右出し→上辺入り）は、
      遠い相手ほど出口辺の外側(手前=上/左, frac小)から出す よう exitFrac を並べ替え＋入口は中央。
      これで各線の縦降口が相手の横線を突っ切らず交差ゼロ（近い相手=辺の下側から短く、遠い相手=上側
      から長く、の入れ子状）。midPos段違いは不要＝自然なL字。軸が平行な群は従来の直線化/中央化/段違い。
■ 検証
  横長6分岐(縦ギャップ小)を実DOMで確認：t1真下=直線、t2/t3=下出し中央着地(段違い)、t4/t5/t6=右辺
  出し(遠いほど上:y=50/40/30)→上辺中央着地。全線 上辺入り・交差ゼロ・箱の貫通なし・角丸維持。
  真横/Y重なりの相手・ピン・ひし形・戻り線は従来動作を保持。
================================================================================

--------------------------------------------------------------------------------
【v8.02 追補2】接続線の扇整形を「向き・役割」対称に一般化（下→上／多対1に対応） 2026-07-01
--------------------------------------------------------------------------------
■ 発端（ユーザー指摘）
  ① 追補1の入口整形が「上→下」だけで、下→上（始点が下・相手が上）では機能しない。
  ② 上→下でも「複数シェイプ→1シェイプ(終点が扇の中心)」だと、1対多を線対称にしたルートに
     ならない（多対1が未対応）。
■ 原因
  A) allocatePorts の扇整形/入口整形が「始点(exit)を hub」に固定＝多対1(終点hub)を扱えない。
  B) 直交群の並べ替え(遠い相手ほど外側)の向きが下向き決め打ち＝上向きで逆転し交差。
  C) 入口整形と扇整形が isReturnEdge(縦モードで上向き=戻り線)を除外していたため、下→上の流れ
     全体が「戻り線」とみなされ整形対象外になっていた（これが①の主因）。
■ 対処
  ・processFan(eds, role) に一般化：role='exit'(hub=始点)/'entry'(hub=終点)をアクセサで切替え、
    上下左右いずれの辺・どちらが扇の中心でも同じ手続きで整形。各辺を「始点側が扇(srcFan)なら
    exit、終点側が扇(tgtFan)なら entry」でグループ化し、多対1も1対多の鏡像として処理。
  ・直交群の並べ替えは脚の向き(leg=相手の主軸方向の符号)で分岐＝上向き/下向きどちらも交差回避。
  ・isReturnEdge 除外を撤去（整形対象に含める）。ブリッジ描画は renderEdges 側の別フラグ(isReturn)
    で独立に動くため、扇整形に含めても前方線を跨ぐ戻り線のブリッジは従来どおり描かれる。
    U字(exitSide===entrySide)・ひし形は引き続き対象外。真横/Y(横モードはX)重なりの相手も従来のまま。
■ 検証（実DOM・6分岐＋戻り線）
  A上→下 / B下→上 / C多対1・下 / D多対1・上 の4形をレンダリング：いずれも真正面の相手は一直線、
  他は主軸の辺(上/下)へ中央着地、近い=手前/遠い=奥の段違いで交差ゼロ・箱の貫通なし。多対1は1対多の
  線対称。前方チェーン＋戻り線(C→A)も右へ回り込み前方線を避けて描画・console エラー0。
================================================================================

--------------------------------------------------------------------------------
【v8.03】マインドマップ機能拡大（合意状態・属性・レベル/フォーカス・テキスト入出力） 2026-07-08
--------------------------------------------------------------------------------
■ 発端
  参考資料「マインドマップアプリ開発参考資料.md」（ユーザー提供）に基づく機能拡大。
  資料の結論=差別化の核は「考えを増やす」より「考えを検証・収束させる」機能（合意状態の
  可視化・メタデータ）であり、これらはサーバ不要＝単一HTML完結方針と相性が良いと判断。
  AI生成・共同編集・クラウド連携系は設計方針と合わないため対象外（ユーザー合意）。
  当初は4小版に分割予定だったが、ユーザー指示で4機能を v8.03 に一括実装。

■ 設計上の判断（why）
  ・ノード属性は optional プロパティ（status / importance / confidence / effort）。
    snap()/saveHtmlCore() はノードを丸ごと直列化するため、追加コードなしで保存・undo・
    後方互換（旧ファイル=未設定）が成立。マイグレータ不要。資料4-1章の「属性データ設計は
    後付け困難」への回答として、まずデータ土台を作り全機能を同じ土台に載せた。
  ・バッジ類（状態=右上円 / 重要度=左縦バー / 確信度=枠線の線種 / 工数=右下ピル）は
    属性スタイル（fill/stroke直書き）で描く＝buildExportSvg はライブSVGの clone なので
    PNG/SVG/印刷へ無改修で反映される。実装前にHTMLモックで見た目をユーザー承認済み。
  ・減光（未決強調・属性絞り込み・フォーカス）は CSS クラス .mind-dim 方式。書き出しSVGには
    ページCSSが載らないため「画面では減光・出力はフル」が自動成立。防護として clone から
    クラス除去も追加。3機能は mindDimSet() の積集合に一本化。
  ・強調/絞り込み/フォーカスは実行時のみの表示状態（snap対象外・図種切替でリセット）＝
    undo履歴を汚さず、保存ファイルにも残さない。
  ・レベル切替は既存 collapsed の一括設定（Space折り畳みと同一機構）。undo可・保存に載る。
  ・FreeMind(.mm)書き出しは独自属性 *_BDM で完全往復＋組込アイコン（button_ok/help/
    messagebox_warning/full-1..3）併記＝FreeMind上でも状態・重要度が見える。
  ・取り込みは共通ビルダー mindBuildFromItems を新設し generateFromOutline 本体は不変更
    （既存アウトライン取り込みへの回帰リスク回避）。不正XMLは DOMParser の parsererror
    検出で {ok:false,error} を返し状態不変。

■ 落とし穴と対策
  ・サイドバーのラベルCSSがガント専用セレクタ（#gantt-settings label）だったため、
    #mind-settings 新設欄のラベルが崩れた。→ セレクタに #mind-settings を追加。さらに
    チェックボックス行は ID セレクタの column 指定が .gchk(row) に勝つため
    #mind-settings label.gchk{flex-direction:row} を明示。
  ・タイトルバー/<title> の版表記が v8.01 のまま残っていた（v8.02 更新漏れ）→ v8.03 で是正。

■ 既知の注意
  ・Markdown書き出しの【タグ】（状態・属性）は、アウトライン取り込みでは文字列として
    扱われる（属性の往復には OPML / .mm を使う）。
  ・テキスト書き出しは折り畳み中の枝も含む全ノードが対象（モーダルに注記あり）。
  ・減光(.mind-dim)は画面表示専用。出力に減光を残したい場合は将来オプション化する。
================================================================================
================================================================================
【v8.04】マインドマップのシェイプ拡充（問題/対策/補足情報）＋重要度表現改良＋凡例 2026-07-08

■ 発端
  ユーザー要望4点: ①シェイプは「トピック」1種のみ→「問題」（改称）＋「対策」「補足情報」を
  新設したい ②凡例を表示したい ③重要度は左バーでなく輪郭内側の二重赤線（太さで高中低）に
  したい ④重要度=高はシェイプ自体・文字も大きく／初期編集エリアが狭いので拡大したい。

■ 仕様決定（HTMLモックで反復し合意）
  ・対策=矢印タグ型（案B・ユーザー選択。案A=カプセル型は不採用）。
  ・補足情報=付箋型・実線枠（案A）。案B=吹き出し型は「破線枠が v8.03 の確信度=中/低の
    枠線種と紛らわしい」ため不採用（重要な選定理由）。付箋は枝色に染まらず薄黄固定・
    文字ひと回り小・枝は細い点線＝「本筋でない注記」を形と線種の両方で区別する。
  ・重要度の赤内枠は低・中も赤のまま（太さのみで区別）とユーザー確認済み。

■ 設計上の判断（why）
  ・「トピック」→「問題」は type='mind_topic' のまま名称のみ変更＝既存データ完全互換。
    新シェイプは新 type（mind_action / mind_note）を追加し isMindShape を拡張。
  ・シェイプ描画は mindShapeEl(n,attrs,inset) に一本化。inset>0 で内側平行輪郭を返すため、
    重要度の二重赤線が3形状（角丸/矢印タグ/付箋）すべてに同じコードで載る。
  ・重要度=高の拡大は calcMindNodeSize 内で実施（毎render再計算されるため設定即反映・
    保存データはサイズを持たない＝互換影響なし）。文字は mindNodeFont() で +2px。
  ・凡例は renderMindLegend() で g-dividers に描画（ライブSVGの一部なので PNG/SVG/印刷へ
    無改修で反映）。ガント凡例と同思想で「図内で使用中のシェイプ・記号だけ」を自動表示。
  ・初期編集領域はマインドマップのみ 1600×1000（他図種は MIN_CANVAS=800 のまま＝回帰回避）。
    中心トピック初期位置も (360,260)→(725,476)=新領域の中央へ。
  ・OPML/.mm はシェイプ種別を独自属性（_shape / SHAPE_BDM）で完全往復。Markdown は
    【対策】【補足】タグを書き出し（読み戻しなし・従来どおり OPML/.mm を推奨）。

■ 既知の注意
  ・矢印タグ型の合意バッジ・工数ピルは先端を避けて MIND_ARROW_TIP(12px) 内側に描く。
  ・補足情報の付箋は選択枠・確信度の線種変更は輪郭に適用される（折れ角は装飾のまま）。
  ・serve.js の既定配信先が v8.03 更新漏れだった（v8.02 のまま）→ v8.04 で是正。
================================================================================
================================================================================
【v8.04 追補】マインドマップ：ダブルクリックでの名称編集が実マウスで成立しないバグ修正 2026-07-08

■ 症状
  マインドマップの各シェイプ（中心/問題/対策/補足情報）をダブルクリックしても
  名称編集エディタが開かない（実マウス環境で再現）。

■ 原因（why）
  mindNodeMD（mousedown）で即 render() ＋ mouseup でも未移動クリックで無条件 render()
  していたため、1回目のクリックで対象の <g> 要素がDOMごと作り替えられ、2回目のクリックが
  「別の新要素」に当たって dblclick イベントが成立しなかった。
  ガント行・汎用ノードで v7.05 追補時に対策済みだった既知パターンの適用漏れ（v8.03 の
  マインドマップ新設時に mindNodeMD を独自実装したため対策が入っていなかった）。

■ 対策（how）
  ガント/汎用ノードと同方式に統一（3点）:
  ①mousedown（mindNodeMD）で即 render しない ②mouseup の未移動クリックは選択描画を
  220ms 遅延（nodeClickTimer・ダブルクリックと排他） ③dblclick ハンドラ冒頭でタイマー解除。
  ドラッグ移動・再ペアレント・単クリック選択は回帰なしを確認済み。

■ その他
  ・serve.js を PORT 環境変数対応に（プレビューのポート競合時に自動割当できるように）。
================================================================================
【v8.04 追補②】マインドマップ：パレット連続配置時に起点（選択）が新シェイプへ移る挙動を変更 2026-07-08

■ 発端
  パレットから1つ配置すると選択（＝次の配置の起点）が新シェイプへ移ってしまい、
  同じシェイプから連続して枝分かれさせられない（ユーザー要望）。

■ 対策（1行・仕様変更）
  mindPlace の配置後選択を S.sel=[n.id]（新シェイプ）→ S.sel=[parent.id]（親のまま）に変更。
  連続配置で同じ親からどんどん分岐する。別の起点にしたい時はそのシェイプをクリックして
  から配置すればよい（従来どおり）。

■ 注意（トレードオフ）
  配置直後に属性ボタン（状態・重要度など）を押すと親に適用される（従来は新シェイプに
  適用された）。新シェイプへの属性設定は当該シェイプをクリック選択してから行う。
  Tabキーでの子追加（mindAddChild）は従来どおり新シェイプ選択＋名称編集を開く（変更なし）。
================================================================================
【v8.04 追補③】マインドマップ：シェイプ内テキストの改行対応（Alt+Enter） 2026-07-09

■ 発端
  シェイプ内のテキストを複数行にしたい（Alt+Enterで改行したい）というユーザー要望。
  版アップ不要の指示（小変更）。

■ 実装（why / how）
  ・エディタ: openSvgEditor に opts.multiline を追加（複数行エディタ強制）。Alt+Enter＝
    カーソル位置に改行挿入・Enter＝確定は従来どおり（既存の enterNewline 方式とは別軸で共存）。
    mindEditNode は常に multiline で開く（表示高さ最低46px）。
  ・描画: 複数行は <tspan> で行ごとに描画（ライブSVGの一部なので PNG/SVG/印刷へ無改修で反映）。
    1行のときは従来どおり textContent（デグレ回避）。
  ・サイズ: calcMindNodeSize を複数行対応（幅=最長行・高さ=基本高+行数×行送りfs*1.3）。
  ・出力の往復: xmlEsc に改行→&#10; を追加（XMLの属性値正規化で literal 改行はスペースに
    潰れるため。&#10; なら OPML/.mm の再取込で改行が復元される）。
    Markdown 出力は改行→スペース（アウトライン構造の破壊防止・MD取込はもともと非往復）。

■ 検証（ブラウザ実機）
  ・Alt+Enterで挿入→Enter確定→2行tspan表示・シェイプ高さ自動拡張（32→48等）を確認。
  ・全シェイプ（中心/問題/対策/補足情報）で複数行表示・1行ノードは従来描画のまま。
  ・OPML/.mm 出力に &#10; が入り、DOMParser 再解析で改行復元・XML妥当性OK。

  ──────────────────────────────────────────────────────────────────────────────
  v8.05（2026-07-09）配置と選択の分離（配置直後に選択しない・余白クリックで解除優先）
  ──────────────────────────────────────────────────────────────────────────────

■ 発端
  ①パレットから新規シェイプを配置すると配置したシェイプが選択状態になるのをやめたい、
  ②選択中に余白を左クリックしたら選択解除だけしてほしい（配置モードでもその1クリックでは
  配置せず、2回目以降は通常どおり1クリックで配置）というユーザー要望。

■ 設計上の判断（why）
  ・対象は「配置」経路のみ＝placeShape系（パレットのクリック配置／ドラッグ配置／ダブルクリック
    追加／グループ枠2クリック／クロスレーン／ガント配置）。addNode等の S.sel=[n.id] を S.sel=[] に変更。
  ・ボタン式の連続追加は対象外＝選択が「続けて追加する起点」として機能している既存のユーザー要望
    仕様のため維持（組織図の部下追加＋= v7.03追補で親選択固定／フロー図の＋／マインドマップの
    Tab/Enter追加＝追加ノード選択→即編集）。シェイプ置換（v5.02）も「配置」でないため選択維持。
  ・マインドマップのパレット配置は「選択解除優先」の例外とした。mindPlace は選択ノード＝次の配置の
    親（v8.04追補②・ユーザー要望）であり、解除優先にすると「子を特定ノードに配置」が不可能になる
    （1クリック目で常に解除→2クリック目は中心直下固定になる）ため。svgMD の解除分岐を
    S.diagramType!=='mindmap' でガード。
  ・ガントは解除優先を適用。クリック配置の挿入行はクリックY位置の行が基準になるため選択アンカー
    なしでも自然に機能する（選択タスク直下挿入・フェーズ初期メンバー指定はドラッグ配置なら従来どおり）。

■ 検証（ブラウザ実機・全図種）
  ・フローチャート/スイムレーン/ガント: 配置直後 S.sel=[]、選択中の余白クリック=解除のみ
    （ノード数不変）→2回目のクリックで配置、パレット非活性時の余白クリック解除も従来どおり。
  ・グループ枠2クリック配置・ダブルクリック追加も配置後に非選択。
  ・マインドマップ: 親選択のまま連続配置・子ノード選択→その配下へ配置、いずれも従来どおり動作。

  ──────────────────────────────────────────────────────────────────────────────
  v8.05 追補（2026-07-14）ガント色追従／樹状図の行追加バグ／マインドマップ3件（版据置）
  ──────────────────────────────────────────────────────────────────────────────
  ※ ユーザー指示によりバージョンは上げず、diagram_v8.05.html を直接更新した追補。

■ ①ガント: フェーズの括り変更でタスク色を自動追従
  ・従来 ganttSyncPhaseTaskColors は「フェーズ色を変えたとき」だけ配下タスクへ色をコピーし、
    タスクの所属フェーズ（phaseId）が変わっても色が旧フェーズのまま残っていた。
  ・ganttSyncAllTaskColors を新設し、括りが変わる2経路（ganttApplyPhaseRange=フェーズ端ドラッグ／
    ganttReorderTo=行並べ替え）の末尾で全タスクの色を現在の所属フェーズ色へ再計算。
    どのフェーズにも属さなくなったタスクは色指定を削除して既定色へ戻す（ユーザー選択）。

■ ②樹状図: 「ラベル有ノードを通常ノードで上書き」後に編集ダイアログの行追加が効かないバグ
  ・原因（why）: confirmOrgEdit が保存時に空行を rows.filter(r=>r.some(c=>c!=='')) で除去していた。
    列は _orgEditCols で管理され空列でも残るのに、行だけ空だと消える非対称が症状の正体。
    → 追加した行を空欄のまま「適用」すると消え、「行が増やせない」ように見えていた
    （文字を入れれば残る）。plain/非plain 両分岐の空行フィルタを削除し、行も列と挙動を揃えた。
  ・切り分け: ラベル有(2段)→通常(1段plain)への置換は replaceNodeType 経由で正しく行われており、
    ダイアログ側の保存ロジックが原因だった。ブラウザ実機（Playwright）で
    上書き→行追加→空欄適用→再オープンし、2行が保持されることを確認。

■ ③マインドマップ: 補足情報(付箋)の色を「属するシェイプ(親)の色」に
  ・従来は付箋を薄黄固定（枝色に染めない）にしていたが、属する枝と対応づけたいとの要望。
  ・mindNodeColor / mindNoteOwnerColor を新設。付箋は親ノードの表示色を基準に、
    枠線・文字・折れ角・接続線を枝色、塗りを淡い枝色(ganttLighten 0.85)に。

■ ④マインドマップ: 複数選択したシェイプのまとめ移動
  ・mindNodeMD が押下時に S.sel=[id] で選択を単一化していたため、まとめ選択しても掴むと崩れた。
  ・既に複数選択中でその一部を掴んだ場合は選択を保持し、選択中の全ノード＋各サブツリーの子孫を
    まとめて移動。付け替え(reparent)は対象が曖昧になるため複数移動時は無効（単独ドラッグのみ）。

■ ⑤マインドマップ: 重要度によるサイズ差を拡大
  ・従来は importance=高 のみ拡大し中/低は同一だった。MIND_IMP_SIZE で三段階
    （高=文字+4/幅余白44/高さ×1.35, 中=+1/32/×1.1, 低=-2/18/×0.82）に。
    重要度未設定のノードは基準サイズ（+0/26/×1.0）のまま。
    高145×43 / 中114×35 / 低81×26 / 未設定102×32px（6文字時）。差の程度はモックで合意。

================================================================================
【v8.06】2026-07-21 — ガント「時（1時間ごと）」モード追加
================================================================================
■ 目的・経緯（why）
  ・ユーザー要望「ガントに1時間当たりの工程モードを追加。ただし既定は従来どおり1日モード」。
  ・仕様は着手前にモック提示＋質問で確定：24時間通し／複数日OK／1時間刻み（分は常に:00）。
    → 稼働時間帯の絞り込みや夜間スキップは行わず「連続タイムライン」を採用。日モードの比例配置
      ロジックをそのまま1時間解像度へ落とすだけで済み、実装リスクを最小化できるため。

■ 設計判断（how）
  ・日時の持ち方: タスク/マイルストーンの startDate/endDate/date を、日モード等は従来の
    `YYYY-MM-DD`、時モードでは `YYYY-MM-DDTHH:00` に拡張。時刻なしは 00:00 とみなす（後方互換）。
    → S.gantt に新フィールドを足さず scale:'hour' だけで表現。snap/restore/init/import は
      既存の Object.assign(defaultGantt(),…) 経由で自動 round-trip、旧ファイルも無改変で開ける。
  ・コアは ganttParseDate の正規表現に任意の時刻部を追加し、書き込み側だけ scale 対応の新関数
    ganttFmt(d,scale) を通す（ganttFmtDate は日付のみのまま据置＝日付入力欄・週/月原点整形を壊さない）。
    ganttAddUnit/ganttDiffUnit に hour 分岐、ganttXToDate を ganttFmt 化。
  ・終了時刻は「排他」に決定（例 9:00〜12:00＝3時間ぶん・12:00 の格子で終端）。日モードの
    「終了日+1日（包含）」とは意味論が異なるため、calcGanttPositions・フェーズ帯・Excelバーの
    3か所で時モードのみ +0（排他）に分岐。モックで提示した見え方（9〜12＝3枠）と一致させた。
  ・編集ダイアログは時モードのときだけ日付入力を datetime-local(step=3600) に切替え、
    保存時に分を :00 へ丸める（ganttInputVal / ganttNormInput）。
  ・Excel: サブ列構築に hour 枝（1時間＝1列・上段=日付/下段=時）。colDateIdx は時刻対応 parse で自動追従。

■ 既知の制約（注意）
  ・休工期間(suspensions)は日単位のまま（編集は type=date を維持、内部の +1日 計算も据置）。
    時モードでも休工は「日」粒度で穴が空く。実害は限定的と判断しスコープ外とした。
  ・既定の完了日は月末(日付)のため、時モードに切替えると 24h×日数ぶん（最大数百列）に広がる。
    重い場合は「完了日」を絞る運用。Excel はサブ列 CAP=500 で保護。

■ 検証（how tested）
  ・追加パッケージ不要（npm レジストリはポリシーで 403）。プリインストール Chromium を
    CDP + Node22 組込み WebSocket で駆動し実機確認：
    既定=日モード／時へ切替(colW=24)／9:00〜12:00 タスクが3列幅で描画／ヘッダー上段=日付・下段=時／
    ganttXToDate 往復一致・時差=3／編集ダイアログが datetime-local／日モードへ戻して回帰なし／
    Excel 出力(時モード)が例外なく完了。全工程で console エラー 0。

────────────────────────────────────────────────────────────────────────────────
【v8.06 追補】2026-07-21 — 印刷(PDF)の時間軸ヘッダーが2ページ目以降に出ない不具合を修正
────────────────────────────────────────────────────────────────────────────────
■ 症状: ガント/工程表の「印刷」出力で、日付・時間の時間軸ヘッダーが1ページ目にしか出ず、
  2ページ目以降はタスク行だけになる。時/日/週/月すべてのスケールで再現（縦方向のページ分割の
  問題のためスケールに依存しない。v8.06 の新機能が原因ではなく従来からの印刷処理のバグ）。
■ 原因(why): 印刷用HTMLは工程表ヘッダーを <thead>(display:table-header-group) に入れて各ページ
  先頭で繰り返す設計。ところが本体(タスク行)を <tbody> の巨大な単一 <tr> にしていたため、
  1つの <tr> が用紙高さを超えて分割されると <thead> が再描画されない（thead の繰り返しは
  「行と行の境目」でしか起きず、単一行の途中改ページには効かない）。フェーズ無しの図では全タスクが
  1 <tr> になり必ずこの状態になる。
■ 対策(how): expPDF のガント分岐を再構成。
  ①各ページ先頭で繰り返すのは「時間軸＋列見出し」だけに限定（表題部＝工事名/図面名帯は pg-once で
    表の外側に一度だけ描き、1ページ目のみ表示）。ユーザー確定＝2ページ目以降は時間軸のみでよい。
  ②本体を「フェーズのまとまり＝1<tr>」または「フェーズ外タスク＝1ページ相当の高さごとのチャンク＝1<tr>」に
    分割。改ページが必ず行境界で起きるようになり、<thead> が全ページ先頭で再描画される。
    ※各 <tr> は SVG 全体を1回直列化するため、当初「1タスク=1行」にしたら直列化がタスク数の二乗コストになり
      printToPDF が固まった（実ブラウザ印刷でも同様）。そこでチャンク高さ maxChunkPx=(W/倍率)×0.7 でまとめ、
      直列化回数をページ数程度に抑えた（W=チャート幅・倍率＝印刷倍率。横長チャートはチャンク大＝ページ少、
      行が支配的なら小＝ページ毎に分割されヘッダー反復。0.7=A系横向きの縦横比を下限に）。
  ③フェーズの切れ目では pg-break(page-break-before) で必ず改ページ（フェーズは途中で分割しない。
    フェーズ名の見た目を保つ。ユーザー確定）。末尾の空き行は従来どおり印刷しない。
■ 既知の注意: 1フェーズが1ページ高さを超える稀なケースは、そのフェーズの2ページ目にヘッダーが
  出ない（フェーズを途中分割しない方針の副作用として許容）。ヘッダーは全幅を用紙幅に合わせる関係で
  縦に圧縮されるが、これはページ1も同条件（従来からの挙動。必要なら印刷倍率を上げる）。
■ 検証(how tested): npm 不可のためプリインストール Chromium を CDP+Node22 WebSocket で駆動し、印刷用HTMLから
  実 PDF を生成→PDFビューアでページ2以降を目視。フェーズ無し(時/日/週/月)＝全ページ先頭に時間軸ヘッダーが
  反復・表題部は1ページ目のみ。フェーズ有り＝各フェーズが自ページ始まり・ヘッダー反復・フェーズ色帯/名は保持。
  thead スライスを単体描画し濃色帯＋日付＋時刻(0〜23)が正しいことも確認。印刷HTMLは0.4〜0.6MBで固まらない。

────────────────────────────────────────────────────────────────────────────────
【v8.07】2026-07-21 — ガント休日グレーアウトの刷新（時モード夜間帯／土日祝チェック／祝日自動判定）＋2改善
────────────────────────────────────────────────────────────────────────────────
■ 背景・要望（why）
  ・時（1時間）モードでも土日祝が「終日」グレーで、稼働時間帯まで塗り潰され実態に合わない。
    → 時モードは夜間 20:00〜翌6:00 のみを非稼働として薄グレーにしたい（毎日）。
  ・休日は現場ごとに異なるため、土/日/祝を選べるようにしたい。ONにした曜日/祝日は全モードで終日グレー。
  ・追加要望2件: ①時モードの初日を必ず 0:00 起点で表示。②グレーアウトでフェーズ帯の色が途切れる。
■ 設計（how / なぜその形か）
  ・グレーアウトの二層化: 「夜間帯(night)」＝時モードのみ・全日 20-6・薄トーン(GANTT_SHADE_NIGHT #f3f6f9)。
    「終日(full)」＝holidays でONの土/日/祝・従来トーン(GANTT_SHADE_FULL #eef2f6)。full が night より優先。
    夜間帯は「チェックに依らず常時」＝毎日の非稼働時間の表現。full は休日設定に従うので両者は別概念。
  ・holidays 既定は {sat,sun,jp} 全 true（新規は土日祝グレー）。holidays 未設定の旧ファイルは
    ganttDayIsHoliday が土ON・日ON・祝OFF に評価＝従来（土日網掛け・祝なし）と後方互換。
  ・祝日は内蔵ロジックで自動判定（外部取得なし＝単一HTML原則を維持）。固定祝日＋ハッピーマンデー(第n月曜)＋
    春分/秋分(概算式・1980〜2099有効)＋振替休日(日曜祝日の連鎖の翌非祝日)＋国民の休日(祝日に挟まれた平日)。
    ラベル赤字は ganttIsRedLabel＝「土日 or 祝日」で常時（チェック非依存＝事実としての休日表示）。
  ・週/月モードは1列が粗い(週=2日刻みExcel／画面は週/月列)ため、画面グレーアウトは従来どおり非対象。
    Excel は既存の日ベース細分化(v6.07)に相乗り＝土日/祝の終日＋時モードは夜間帯も塗り分け(full/night)。
  ・UI はユーザー指定によりサイドバーでなく上部ツールバー(#gantt-tools)の「表示」右隣に土/日/祝チェックを新設
    （モックで配置・挙動を事前提示し承認済み）。ganttSetHoliday でトグル→render、updateGanttToolbar で同期。
■ 追加改善の how
  ・時モード初日0:00: ganttAxisOrigin() の hour 分岐で開始日の時刻を切り捨て（ganttFmtDate で当日00:00を原点に）。
  ・フェーズ帯の途切れ: フェーズ・ヘッダー行の時間軸部分は最背面(g-lanes/renderGanttRows)に薄フェーズ色で
    塗るため、その上の g-grid のグレーアウトで色が抜けていた。renderGanttGrid 末尾でフェーズ行の時間軸部分を
    グレーアウトの上へ薄フェーズ色で再描画し帯色を優先（左情報欄側は従来どおり renderGanttNameCol が担当）。
■ 既知の制約・注意
  ・週/月モードの画面グレーアウトは列粒度が粗いため非対応（従来どおり）。休日チェックの効果は日/時で確認できる。
  ・祝日の概算式(春分/秋分)は 1980〜2099 年で有効。天皇誕生日は現行(2/23・2020〜)のみ対応（過去の12/23は非対応）。
■ 検証（how tested）
  ・npm 不可(レジストリ 403)のためプリインストール Chromium を CDP + Node22 組込み WebSocket で駆動し実機確認：
    console/exception エラー 0。時モードで origin=当月初日・初日0:00開始。土日祝ON＝土日終日グレー＋平日夜間帯
    (0-5/20-23)薄グレー、土日/祝ラベル赤字、フェーズ帯が土日列でも連続。土日OFF＝土日も夜間帯のみ。
    ganttSetHoliday でholidays更新→再描画。日モードへ切替も回帰なし。
  ・祝日判定の単体テスト(Node)で 2026 の 元日/成人/建国/春分/昭和/憲法/みどり/こども/海の日/山/敬老/秋分/
    スポーツ/文化/勤労感謝＋振替休日＋国民の休日 を確認（海の日=7/20・国民の休日=9/22 等が一致）。

────────────────────────────────────────────────────────────────────────────────
【v8.07 追補】2026-07-22 — 印刷(PDF)のページ分割を「フェーズ毎に必ず改ページ」→「途切れる時だけ改ページ」に戻す
────────────────────────────────────────────────────────────────────────────────
■ 経緯（why）: v8.06追補で入れた「フェーズの切れ目で必ず改ページ(pg-break=page-break-before:always)」が
  ユーザーの意図と異なっていた。本来は「基本は極力1ページにまとめ、フェーズがページ境界で途切れる場合だけ改ページ」
  （＝従来仕様）が望ましい。ユーザー指示によりバグ修正として直接 main へ反映（版番据置の追補）。
■ 対策（how）: 2段構え。
  (a) expPDF のフェーズ<tr>への強制改ページ(pg-break=page-break-before:always)を廃止（phaseBreak を false 固定）
      ＝フェーズ毎に必ず改ページするのをやめ、基本は1ページにまとめる。
  (b) ただし pg-break を外しただけでは、1フェーズが用紙高さを超えると<tr>が途中で分割され、<thead>の日付
      ヘッダーが2ページ目以降で反復されず消える（v8.06追補の元バグと同じ構造。当初の追補はここが不完全だった）。
      そこで flushPhase を修正し、フェーズの<tr>も maxChunkPx(≒用紙1ページ)ごとに分割 emit（while ループ）＝
      全<tr>が用紙高さ以下になる。
  結果、強制改ページはせず（page-break-inside:avoid でフェーズ/チャンクがページ境界で途切れる時だけ改ページ）、
  かつ各<tr>が用紙以下なので <thead>(table-header-group)が全ページ先頭で反復し日付ヘッダーが消えない。
  <thead>の全ページ反復・チャンク分割・表題部1ページ目のみ(pg-once)は v8.06追補のまま。未使用化した CSS
  ルール .pg-break は残置（無害・コメントで不使用と明記）。
■ 検証（how tested）: 構文チェック(script 2本・エラー0)。プリインストール Chromium を CDP+Node22 WebSocket で
  駆動し印刷HTML/PDFを生成して確認：thead に日付ヘッダー(SVG height=56)を含む／大フェーズ(タスク50)で本体が
  6ブロックに分割され各<tr>高さ≤1584(=maxChunkPx・用紙1ページ相当)以下／pg-break 付与0／--print-to-pdf で
  2ページPDF生成。各<tr>が用紙以下のため <thead> が全ページ反復＝日付ヘッダーは全ページに出る。
  ※headless環境に pdftoppm 不在のため PDF の画素目視は不可。構造・高さ・ページ数で保証（実機の印刷プレビュー推奨）。

────────────────────────────────────────────────────────────────────────────────
【v8.07 追補②】2026-07-22 — 印刷(PDF)の時間軸ヘッダーが2ページ目以降に出ない不具合を「実機PDF目視」で再修正
────────────────────────────────────────────────────────────────────────────────
■ 経緯（why）: v8.06追補・v8.07追補で「各<tr>を用紙1ページ以下に分割すれば <thead>(display:table-header-group)
  が全ページ先頭で反復する」という前提で対策したが、ユーザーから「2ページ目以降にヘッダーが出ない」と再報告。
  当時の検証は構造・高さ・ページ数のみ（PDF画素目視は不可と明記）で、実際の印刷結果を見ていなかった。今回、
  プリインストール Chromium(Playwright) で実PDFを生成し各ページを画像化して目視した結果、前提が2点で崩れて
  いたことを特定した。
■ 真の原因（root cause）: 2つが重なっていた。
  (1) チャンク高さ係数が用紙に対して過大＝ヘッダー分を差し引いていなかった。maxChunkPx=0.7×W/倍率 は
      A/B系横向き(内容比≒0.707)でレンダ高≒0.7×内容幅。各ページ先頭で反復するヘッダー(≒13mm)が上に載ると
      「ヘッダー＋1チャンク」が用紙(≒190mm@A4横)を僅かに超過し、その<tr>が用紙をまたいで分割→続きページで
      <thead>が反復されず消える。※実PDFで再現（p2のみヘッダー、p3〜はヘッダー無しで本体だけ）。
  (2) 【本命】要素数の多いガントSVG(≈860要素の時間軸ヘッダー slice)を <thead> に直接置くと、各<tr>が用紙
      以下でも Chromium が2ページ目以降でヘッダーを反復描画しない。＝(1)を直しても(0.60へ下げても)ヘッダーは
      p2のみのまま。単純図形の placeholder SVG に差し替えると全ページ反復する／同じ複雑さの本体SVGは通常フロー
      なので各ページ正常描画される→「複雑SVGを反復<thead>に置く」こと自体が引き金と切り分け。<defs>(filter6/
      marker15)を除いても再発したため、特定要素ではなく要素規模/描画コストに起因する Chromium 側の挙動。
■ 対策（how）: 2段構え。
  (a) チャンク上限を是正: maxChunkPx=Math.max(300,(W/倍率)×0.60−headerPx)。反復ヘッダー高さ(headerPx=
      ganttBodyTop−ganttHeadH≒56)を差し引き、係数0.7→0.60で A/B系横向き用紙に安全余裕（レンダ高「ヘッダー＋
      チャンク」≒0.60×内容幅≦0.707×内容幅）。CSS auto-flow の詰め込みは各<tr>のみ小さくしても効くのでページ数は
      無駄に増えない。
  (b) 【本命】反復ヘッダーをラスタ化: 時間軸ヘッダー slice を高解像度PNG(canvas, targetW=clamp(W×2,1600..6000))
      へ描き、<thead> には <img class="pg-hdr"> を置く（本体はSVGのまま）。ラスタ<img>は反復描画が確実。
      失敗時は従来のSVG直列化にフォールバック（最悪でも1ページ目は出る）。
  ・付随: ラスタ化(await)を挟むため expPDF を async 化し、ポップアップ(window.open)は同期のうちに開いて
    「生成中…」を表示→完成後に本HTMLへ差し替え（ポップアップブロック回避）。CSS に .pg-hdr{width:倍率%…} を追加。
■ 検証（how tested）: プリインストール Chromium(Playwright) で実PDFを生成し各ページ上部を画像目視。
  ・切り分け: 反復<thead>に「複雑SVG=p2のみ／placeholder=全ページ／raster<img>=全ページ」を実PDFで確認。
  ・実機 expPDF: 単一フェーズ30タスク・A4横で before=p2のみヘッダー(p3〜欠落) → after=全ページ(p2〜p6)に
    時間軸ヘッダー(フェーズ/タスク列＋YYYY年M月＋日目盛)が反復、表題部は1ページ目のみ。console例外0。
  ・回帰: 既定シード(6タスク)=1ページに収まりヘッダー1回／見出し欄OFF(図面名帯)=7ページ全body頭にヘッダー／
    月スケール30タスク=6ページ全body頭にヘッダー。いずれも pageerror 0・pg-hdr(img)採用。
■ 既知の注意: ヘッダーは画像化されるため文字の選択/検索は不可（表題部・本体は従来どおりベクタSVG）。
  0.60係数は A/B系横向き前提の安全値（US Legal横=内容比≒0.61 では余裕僅少）。日本の工程表は A/B系のため実害なし。

════════════════════════════════════════════════════════════════════════════════
v8.08（2026-07-25） ガント ①折り返し表示モード ②休日にかかる工程の点線表示
════════════════════════════════════════════════════════════════════════════════

■ なぜ（why）
  ・①折り返し: 時間軸が常に右へ一直線に伸びるため、工期が長いと横に延々とスクロールし、A4に収まらない／
    印刷で読みづらい。カレンダーのように一定の区切りで次の段へ折り返せば横スクロールが不要になる。
  ・②点線: 土日祝の網掛け（v8.07）はあるが、その上をバーが素通りするため「この工程は休日を含んでいる」ことが
    図から読めない。実際の稼働日を図で判断できるようにする。

■ 設計判断（why this way）
  ・折り返しの実装方式＝「段ごとに軸原点を段の先頭へ差し替え、Yは平行移動グループでずらす」。
    これにより既存の renderGantt*（行背景/グリッド/軸ヘッダー/左列/バー/フェーズ）をそのまま再利用でき、
    段対応版の関数を二重に作らずに済む。座標計算は常に「段0のローカル座標」で書ける。
    → ganttAxisOrigin / ganttTotalUnits を _gwBand で切り替え、ganttLayer(id) が段ごとの
      <g transform="translate(0,dy)"> を返す。
  ・許す組み合わせを「列が段の境界にぴったり揃うもの」に限定した（時→日ごと／日→週ごと・月ごと）。
    週/月目盛りは1列が粗く境界に揃わないため対象外。これは v8.07 で週/月の休日グレーアウトを非対象にしたのと
    同じ理由で、仕様の一貫性を保つ判断。
  ・週ごとの帯境界は「日曜始まり」（ユーザー指定）。一方 scale==='week' の目盛り自体は従来どおり月曜基準の
    ままにした（既存図の後方互換を壊さないため）。※注意点として下記に再掲。
  ・段をまたぐバーは矢羽根（chevron）で継続を示す（ユーザー選択）。単純に切ると「そこで終わった工程」に見える。
  ・②の分割は既存の ganttBarSegments（休工でバーに穴を空ける仕組み）を拡張して実現。休日判定も既存の
    ganttDayIsHoliday を再利用し、画面のグレーアウトと必ず同条件になるようにした（設定が1箇所で済む）。
  ・休工＝バーを消す表現／休日＝バーを破線にする表現。役割を分けたので視覚的に競合しない。
  ・バーを rect から path 描画に変更した理由: 休日で区間分割すると rect の rx=4 が内側の継ぎ目にも効いて
    「数珠つなぎのピル」に見え、1本の工程として読めなくなる。端の形を左右別に指定できる path
    （0=平ら/1=角丸/2=矢羽根）にし、塗りと外周線を分けて内側の継ぎ目には線を引かない構成にした。

■ 既知の注意（gotcha）
  ・週ごと折り返しの帯境界は日曜基準、scale==='week' の目盛りは月曜基準。両者は別物なので混同しないこと。
  ・折り返しONでは末尾の空き行を1行だけにしている（段ごとに8行の空き行が繰り返されて縦に膨れるため）。
    「列を追加」＋ボタンも先頭の段にだけ描く（同じボタンが段数ぶん並ぶのを避ける）。
  ・折り返しONの印刷は反復<thead>を使わない（段が自前でヘッダーを持つため）。v8.07追補②のラスタ化ヘッダーは
    折り返しOFFのときだけ通る経路になっている。
  ・Excel出力は折り返しに未対応（時間軸は一直線で書き出す）。expExcel は左情報欄の結合セル・縦書き・freeze・
    印刷設定が連続した1本の時間軸前提で組まれており、段ブロックを縦に積むには関数の作り直しが必要。
    Excelは横スクロールが自然にできるため実害が小さいと判断し、出力時にヒントで明示する形にした。

■ 検証（how tested）
  ・回帰: v8.07 と v8.08（折り返しOFF・休日OFF）を時/日/週/月の4スケールで機械照合し、キャンバスサイズ／
    各レイヤーの要素数／バー座標／テキストがすべて完全一致（フェーズ・休工・マイルストーン込み）。
  ・②: 休工併用・時モード・週モード（点線が出ないこと）・フェーズ併用（フェーズバーに適用されないこと）・
    休日チェック全OFF（点線が消えること）を分割数と実画像で確認。
  ・①: 日→週ごと(7段)／日→月ごと(2段)／時→日ごと(3段・24列)を実画像で確認。段見出し・軸ヘッダー・左列の
    繰り返し、段をまたぐバーの矢羽根、休日破線との併存を目視。
  ・印刷: window.open を差し替えて出力HTMLの構造を検証。折り返しOFF=<thead>＋ラスタヘッダー＋本体1行（従来同）、
    週ごと=ヘッダーなし・7行、月ごと=ヘッダーなし・2行。

── v8.08 追補（2026-07-25／同一版内）折り返しの拡張と「段ごとの行間引き」──

■ なぜ（why）
  ・折り返しが「時→日ごと／日→週・月ごと」に限られていて、週目盛り・月目盛りでは使えなかった（ユーザー要望）。
  ・折り返した結果、その段に工程が1つも無い行（前月で終わったタスクなど）やフェーズ見出しがそのまま並び、
    段数ぶん繰り返されて冗長だった（ユーザー指摘）。

■ 設計判断（why this way）
  ・折り返し単位を「目盛りより1段上」で通した: 週→月ごと、月→年ごと。月目盛り＋月ごとは1段1列で無意味なため
    年ごとを採用（ユーザー確認済み）。
  ・週目盛り＋月ごとは列が割り切れない（1月≒4.43列）。当初は「週が月をまたぐから境界に揃わない」と考えて
    対象外にしていたが、ユーザー指摘のとおり「月ごと折り返しなら1日から月末を出せばよい」。そこで
    **列は切り上げて描き、段の内容を区間の終わり（月末）でクリップする**方式にした。
    - クリップ: clipPath を段ごとに作り、平行移動グループに clip-path を付ける（ganttBandClipId）。
      グリッド・ヘッダー・行背景の個別クランプを全部書くより、はみ出しを1箇所で確実に止められる。
    - バーの切り位置も ganttBandEndX（＝区間の終わり）にした。ceil列の余白に次の月のバーが漏れないため。
    - ヘッダー上段は endX を ganttBandEndX にし、さらに幅0セルを描かないガードを入れた（クリップだけでは
      段の右端に次区間のラベルが半分残る）。**このガードは折り返しON時のみ**にしている＝折り返しOFFの
      描画を v8.07 と完全一致に保つため（OFFで有効にすると月スケールで要素数が2個減って一致しなくなる）。
  ・行間引きで段の高さが段ごとに変わるため、段モデルを「i*固定高さ」から ganttBands()=[{i,dy,H,layout}] の
    一覧方式に作り直した。段ごとに ganttRowSlots(keepIds) で行を絞ったレイアウトを持たせる方式にしたので、
    各 renderGantt* は従来どおり ganttLayout() を読むだけで段ごとの行構成に追従する。
  ・ganttSlotIndexAtY は「段を解決 → その段のローカルY・その段のレイアウトで判定 → 戻り値は全体のタスク
    一覧での index に直す」形にした。段ごとに行を間引くと、その段のレイアウト内の順番＝全体の順番では
    なくなるため、ノード参照から index を引き直す必要がある（ここを数え上げのままにすると挿入位置や
    フェーズ端ドラッグが1行ずれる）。
  ・折り返し時は末尾の空き行を作らない（extra=0）。行間引きで段が短くなったので、空き行が段数ぶん
    積み上がると再び冗長になる。

■ 既知の注意（gotcha）
  ・ganttCurBand() は ganttBands() を呼ばずキャッシュ(_gwBands)だけを見る。ganttBands() の構築中に
    ganttAxisOrigin() 等から間接的に呼ばれると無限再帰になるため（構築中は「折り返しなし」として扱う）。
  ・_gwBands は renderGantt 冒頭で null に戻す。段構成は行・日付・折り返し設定に依存するので、
    状態を変えたら必ず render() を通すこと。
  ・段のクリップ定義（clipPath id=gw-clip-N）は SVG 直下に置くため、renderGantt 冒頭で古いものを削除している。

■ 検証（how tested）
  ・折り返しOFF: v8.07 と時/日/週/月の4スケールで完全一致（回帰なし）を再確認。
  ・週ごと折り返し: 7段→5段（工程の無い週が省略）、SVG高さ 3116→1084px に圧縮。
  ・日目盛り＋月ごと（フェーズ2つ・10月に工程なし）: 7月=[設計]基本設計 詳細設計 [構築]実装 ／
    8月=[構築]実装 テスト（設計フェーズと基本/詳細設計が消える）／9月=[構築]テスト ／10月は段ごと省略 →
    11月=引渡し（フェーズ無し）を実画像で確認。
  ・週目盛り＋月ごと: 各段5列（正味4.43）でヘッダーが当月ラベルのみになり、月末で内容が切れることを確認。
  ・月目盛り＋年ごと: 1段12列。
  ・印刷: 折り返しONの <tr> 数が段数と一致（週ごと=5、月ごと=2）。折り返しOFFは従来構造のまま。

── v8.08 追補②（2026-07-25／同一版内）折り返しの「段の右端」の表示バグ修正 ──

■ なぜ（why）
  ・折り返しONにすると、どのモードでも段の右側の表示が崩れていた（ユーザー指摘「一番右側がすべての
    折返しモードで表示がバグっています」）。原因は3つとも「右端の基準が段ではなくキャンバス全体
    (SVG_W) のままだった」こと。段ごとに軸原点を差し替える方式で既存の描画関数を再利用したため、
    Xを段で切る処理が入っていない箇所が残っていた。
    ① フェーズのロールアップバー（renderGanttPhaseBars）に段のクリップが無く、段の右外まで伸びて
       次の段の領域を横切っていた（タスクバーには v8.08 本体で入れていたが、こちらは漏れていた）。
    ② 時間軸ヘッダーの中段の横線が x2=SVG_W のまま＝段より右へ突き抜けていた。
    ③ フェーズ行の帯（時間軸側の色帯）が width=SVG_W-x0 のまま＝キャンバス右端まで伸びていた。
  ・あわせて、見出し欄ONだと最小キャンバス幅(1120)まで広がるのに1段が狭いままで、右半分が
    まるごと空白になっていた（週ごと折り返しで段=約550px／キャンバス=1120px）。

■ 設計判断（why this way）
  ・「描画してよい右端」を ganttRightEdge() に集約した（折り返し中=段の右端＝区間の終わり、通常=SVG_W）。
    折り返しOFFでは SVG_W を返すので従来の描画と完全に同一。右端を各所で個別に計算し直さない。
    はみ出し文字の右詰め（v5.00②のタスクバー/マイルストーン）もこの基準に載せ替えた。
  ・フェーズのロールアップバーはタスクバーと同じ扱いにした（段の帯で切り、はみ出す側は矢羽根）。
    バーの形は「上のスパイン＋左右端が下へ脚」なので、切った側は脚を出さず矢羽根にする
    ＝脚があると「そこでフェーズが終わった」ように読めるため。切らない側の形は従来と同一。
  ・右の空白は「列幅を広げて段を幅いっぱいに使う」で解消した（ganttComputeColW の折り返し分岐）。
    折り返しは横スクロールを無くすためのモードなので、幅を余らせる意味がない。考え方は v5.02 の
    「期間が短いときは画面いっぱいに列幅を自動拡大」と同じで、ズーム値 colWidth は下限として残す。
    見出し欄OFFのときは元から段=キャンバス幅なので拡大しない。
  ・キャンバス幅・列幅の基準は段の「列数（切り上げ）」ではなく「正味の長さ（小数）」にした
    （ganttWrapMaxBandSpan）。週目盛り＋月ごとは列を切り上げて描き内容は月末でクリップするため、
    切り上げたぶん（最大1列ぶん）がそのまま右の空白になっていた。
  ・依存線（renderGanttEdges）は、両端の行がその段に出ていないときは描かないようにした。
    段ごとに行を間引くので、段に出ていない行の n.x/n.y は他の段の値のままで線が飛ぶ。

■ 既知の注意（gotcha）
  ・ganttXToDate は「最も近い単位境界」へ丸めるスナップ仕様。列の中心をクリックすると次の列の
    日付が返るのが正常。当たり判定の検証は列の左端付近で行う（列中心で期待値と比べると全部ズレて見える）。
  ・折り返しONで列幅が既定より広がるのは意図した挙動（週ごとで1日=114px 等）。ズーム値は変わらない。

■ 検証（how tested）
  ・折り返しOFF: v8.07 と時/日/週/月の4スケールで完全一致（回帰なし）を再確認。
  ・実UIと同じ手順（ganttSetScale→ganttSetWrap）で全5組み合わせを描画し、
    「段の右端＝キャンバス右端」（SVG_W - ganttRightEdge() = 0）を全段で確認
    ＝時+日ごと47段／日+週ごと10段／日+月ごと2段／週+月ごと2段／月+年ごと1段すべて0。
  ・当たり判定: 全モード・全段・全列で 画面座標→段→日付 が一致（day+week 70列・hour+day 1128列 ほか）。
  ・印刷: 折り返しONの <tr> 数が段数と一致（週ごと=5、月ごと=2）＝構造の回帰なし。

── v8.08 追補③（2026-07-25／同一版内）情報欄の文字が列の枠を越える不具合の修正 ──

■ なぜ（why）
  ・時モードで左情報欄の工程行「工程：2026年8月12日 0時～2026年8月30日 0時」が列の枠を越えていた
    （v8.07 から存在。ユーザー指摘）。真因は ganttClip の文字幅の見積り＝一律 fontPx*0.62。
    全角は fontPx 幅あるので、全角が多い文字列を短く見積もり、切られずにそのまま描かれていた。
    実測で v8.07 は最大 18.8px はみ出し（フェーズ名・工程行の複数箇所）。
  ・折り返しONの調査中に、段をまたぐタスクバーのラベルが「切る前の座標 n.x」に置かれており、
    段の左外＝左情報欄の上に文字が乗るケースを発見（実測 34px 侵入）。段の左端との差が
    1〜2列ぶんのときに起きる（差が大きいと画面外へ出るため気づきにくかった）。

■ 設計判断（why this way）
  ・文字幅の測り方を ganttTextW（全角=fontPx／半角=fontPx*0.55）に集約した。基準は見出し欄の
    mLab と同じ＝アプリ内で二重の見積り方式を持たない。ganttClip もこれで測って1文字ずつ積み、
    「…」の幅を残して切る（従来は文字数で割るだけだった）。
  ・工程行は「切って…にする」より「入るフォントまで落とす」を選んだ（ganttFitFs・下限9px）。
    日付は末尾まで読めないと意味がない情報なので、末尾を落とすより1〜2px縮める方が実用的。
    バー内ラベルの自動縮小（v4.30）と同じ考え方。
  ・段をまたぐバーのラベルは、切ったあとの座標（_bx0／_bx1-_bx0）を基準にした。バー本体だけを
    切ってラベルを元座標に置くと、バーは段内・文字は段外という食い違いになる。
    はみ出し文字の右詰めも、折り返しONでは完了日の指定に関係なく効かせる。
  ・フェーズ名がバーに入らないときの「バーの右に置く」フォールバックは、段の右に余地が無ければ
    バー内に白字で入れる（ganttClip で切る）ようにした。追補②で入れた右詰めだけだと
    バーに重なって読めなくなる（段の幅が限られる折り返しONで起きやすい）。
  ・fits の判定式 label.length*12+12 は据え置いた。ここを正確な幅に直すと折り返しOFFでも
    ラベルの内外判定が変わり、v8.07 との完全一致（回帰チェックの土台）が崩れるため。

■ 検証（how tested）
  ・getBBox の実測で「左情報欄の文字が列境界を越えていないか」を全モードで確認。
    長い名称（20字前後）・時刻付きタスク・マイルストーン・概要/担当セルを入れた条件で、
    時/日/週/月 × 折り返しOFF/日ごと/週ごと/月ごと/年ごと すべて 0 件。
    同条件の v8.07 は 4 件（最大 18.8px 超）＝修正の効果を確認。
  ・折り返しOFF: v8.07 と時/日/週/月の4スケールで完全一致（回帰なし）。
  ・段の右端＝キャンバス右端（差0）・当たり判定・印刷の <tr> 数＝段数も再確認して不変。

── v8.08 追補④（2026-07-25／同一版内）情報欄の無記入の項目の空白を詰める ──

■ なぜ（why）
  ・左情報欄のタスク行が常に「タスク名／工程／概要／担当」の4行ぶんの高さを確保していて、
    概要や担当が空でも1行ぶん空いていた（ganttRowMaxLines が最低1を返す）。
    概要を使わない運用だと全タスクに空白の1行が入り、縦に無駄が出る（ユーザー指摘）。

■ 設計判断（why this way）
  ・概要（行3）・担当（行4）のどちらも、無記入なら行を作らない。当初は「担当は空でも1行残して
    縦位置を揃える」で実装したが、ユーザー判断で担当も詰める形に変更（2026-07-25）。
    → 空のタスク行は「タスク名＋工程」の2行ぶんだけになる。
  ・ON/OFFの設定は作らず常時適用（ユーザー選択）。行の高さは v4.04 から内容に応じた可変なので、
    「何も描かれない項目は行を作らない」はその考え方の素直な延長で、状態を増やす価値が薄い。
  ・「そのセルに何か描かれるか」の判定を ganttCellShown に集約した。従来は描画側（renderGanttNameCol）
    の表示条件と高さ計算（ganttRowMaxLines）が別々の式で、片方だけ直すと高さと中身がズレる。
  ・詰めた概要セルには当たり判定（addHit）を置かない（高さ0の矩形は押せないので無意味）。
    このため概要が空の行はインラインでの概要入力口が無くなるが、編集モーダル（タスク名・工程行・
    バーのダブルクリック）に情報欄の項目名/本文の欄があるのでそこから入力できる。入力すれば
    行が伸びて以降はインラインでも編集できる。
  ・末尾の空き行の高さ（ganttEmptyRowHeight）も 4行→2行に合わせた。v5.03 の「空き行＝空のタスク行と
    同じ高さ」という取り決めを保つため。
  ・工程↔情報欄の区切り線と、その下の余白（GANTT_CELL_TOP_GAP=6＋下パディング7＝13px）は
    セルが無い行でも残した。この線は列（概要列＋追加列）の縦区切りの上端でもあり、
    消すと空の行だけ列のグリッドが途切れる（v5.03 が空き行にも内部の区切りを描くようにした理由）。
    残るのは「行1つぶんの空白」ではなく構造上の最小余白。

■ 検証（how tested）
  ・概要・担当に文字が入っている条件では v8.07 と完全一致（時/日/週/月）＝詰めるのは空のときだけ。
    差が出るのは末尾の空き行の高さ（88→54px）のみで、タスク行は88pxのまま・バー座標も文字も同一。
  ・行の高さ: 両方空=54px ／ 片方だけ入力=71px ／ 両方入力=88px。概要を入力すると 71→88px に
    戻ることを確認（入力→再描画で追従）。
  ・片方だけ入力した行のインライン編集が正しい位置に出ること、編集モーダルに概要/担当の欄が
    あることを確認（両方空の行はインラインの入力口が無くなるためモーダルが入口）。
  ・Excel出力・印刷(PDF)・折り返し（段の右端／当たり判定／情報欄のはみ出し）に回帰なし。

── v8.08 追補⑤（2026-07-25／同一版内）折り返し時の凡例を横一列に／フェーズの凡例を実際の形へ ──

■ なぜ（why）
  ・折り返しONではキャンバス幅が1段ぶん（≒最小幅1120）に縮むため、右上に縦積みしていた凡例（幅392）が
    見出しの情報行と横に重なった。A3縦・A4縦は用紙が狭く、印刷でも同じ重なりが出る（ユーザー指摘）。
    実測: 折り返しONで W=1122 のとき「版数：第3版（2026-07-25 改訂・全面見直し）」が x=477..746、
    凡例の箱が x=716.. で衝突。折り返しOFF（W=2308）では起きない＝狭さが原因。
  ・凡例のフェーズの色見本が角丸の帯で、図中のロールアップバー（上のスパイン＋左右端が下へ伸びる脚）と
    形が違いすぎてタスクバーと見分けが付かなかった（ユーザー指摘）。

■ 設計判断（why this way）
  ・折り返しONのときだけ凡例を横一列にし、見出しの下（表題・情報行・承認欄より下）に右詰めで置く
    （ganttHeadModel の lgInline）。折り返しOFFは従来の縦積みの箱のまま＝広いキャンバスでは今の形が
    読みやすく、変える理由がない。
  ・当初は「最上段＝作成日の左」に置いたが、幅を作成日・表題と取り合うため項目が増えると文字が
    縮んでしまう（6項目で既に11→約9px）。ユーザー判断で「見出しの下に右詰め」に変更（イメージ図で確認）。
    こちらは横幅を丸ごと使えるので10項目でも11pxのまま入る。
  ・情報行は「見出しの下端から上へ詰める」配置なので、凡例の行ぶんを含めた H を基準にすると
    情報行が凡例の行へ降りてしまう。表題〜承認欄までの高さ baseH をモデルに持たせ、情報行は
    baseH を基準に詰める（H は凡例の行を含む全体の高さ）。
  ・入る文字サイズは線形に解く。幅＝固定部（余白＋色見本＋項目間）＋文字幅（フォントサイズに比例）なので、
    「必要幅と使える幅の比でフォントを縮める」概算では固定部が縮まらず収まらない（最初この誤りで
    10項目のとき最後のラベルが箱から25pxはみ出した）。下限7pxまで縮め、それでも足りなければ
    項目間を詰め、最後はラベル末尾を…で切って必ず1列を保つ。
  ・箱の右端はキャンバス右端の余白（rEdge=幅-14）に合わせ、左へ伸びる＝右詰め。使える幅は
    左余白(16)〜rEdge の全幅で、他の要素と取り合わない。
  ・フェーズの色見本は実バーと同じ形（上のスパイン＋左右端が下へ伸びる脚）の path にし、比率は
    本体:脚の深さ＝6:11（実バーの15:25と同比）。脚の幅（cap）だけは実バー（幅の3%程度）どおりにすると
    22pxの見本では見えなくなるため誇張している。枠線も実バーと同じ色で入れた。

■ 検証（how tested）
  ・折り返し(週/月/年)×承認欄ON/OFF×情報行2件/10件、さらに凡例10項目の条件＝全19通りで、
    凡例の箱が「表題・情報行・作成日・承認欄」のいずれとも重ならないことを実測（getBBox）で確認。
  ・10項目でも1列を保ち、箱からラベルがはみ出さないことを確認（見出しの下＝全幅を使えるため11pxのまま）。
  ・折り返しOFFの見出し・凡例は従来と同一（v8.07 比で差はタスク行の詰め＝追補④の分のみ。
    バー座標・文字・レイヤー要素数は一致）。印刷(PDF)・Excel出力・当たり判定・情報欄のはみ出しも回帰なし。

── v8.09（2026-08-03）重ね置き入れ替えをマインドマップ＋フローチャートへ拡大 ──

■ なぜ（why）
  ・「フローチャートと同様に、マインドマップでも既存シェイプに重ねてドロップしたら形状だけ入れ替え、
    文章は変えないでほしい」というユーザー要望。調査したところ、この重ね置き入れ替え（v5.02）の対象は
    実はスイムレーン図と樹状図（v7.06）だけで、フローチャート自体は対象外だった。ユーザーの認識に
    合わせ、パレット構成がスイムレーン図と同一のフローチャートも今回まとめて有効化した。

■ 設計判断（why this way）
  ・既存機構（placeReplaceEnabled / isReplaceableShape / replaceTargetAt / replaceNodeType）をそのまま
    使い、対象図種と対象シェイプ（mind_topic/mind_action/mind_note）を追加しただけ。ドラッグ中の
    オレンジ破線の強調表示も既存のまま動く。
  ・mind_root（中心トピック）は対象外＝isReplaceableShape に含めない（中心を対策や付箋にする操作は
    意味がなく、誤操作の温床になるため）。
  ・replaceNodeType はマインドマップ分岐で type の差し替えのみ行う。w/h は毎描画 calcMindNodeSize が
    文章と重要度から再計算するため、ここで触ると二重管理になる。文章・属性（状態/重要度/確信度/工数）・
    枝の接続はプロパティに触れないことでそのまま保持される。
  ・折りたたみ・絞り込みで非表示中のノードは replaceTargetAt で対象外にした（mindHiddenSet 参照）。
    当たり判定 nodeAtPoint は幾何判定のため、ガードが無いと見えない箱への誤置換が起きる。

■ 検証（how tested）
  ・実ブラウザで、パレットからの実マウスドラッグ→既存ノードへドロップで入れ替え（文章保持・ノード/枝数
    不変）を確認。問題→対策/補足、補足→問題の相互、同種重ね＝新規追加、中心トピック重ね＝新規追加、
    非表示ノードは対象外、Undo/Redo、スイムレーン図の従来入れ替えの回帰なし、コンソールエラー0を確認。

── v8.09（2026-08-03）上書き保存の新設（開いたファイルへそのまま書き戻す） ──

■ なぜ（why）
  ・保存が「ダウンロード」方式（dl＝<a download>）のため、保存先を選べずダウンロードフォルダへ行き、同名が
    あると上書きではなく〜(1).html が増える。過去に何度か仕様を詰めても理想形にならなかったのはこの方式が
    原因で、方式を変えない限り上書きは原理的に不可能だった（ユーザー指摘 2026-08-03）。
  ・ユーザー案は「開いた時のディレクトリを記録し、同名ファイルを上書きする」。ブラウザはページへファイルの
    絶対パスを渡さず、パス文字列を指定して書き込むAPIも無いため、この案そのものは実装できない。

■ 設計判断（why this way）
  ・記録するのは「場所（パス）」ではなく「ファイルへの参照（FileSystemFileHandle）」にした。File System Access API の
    ハンドルは一度ユーザーが選べば以後ダイアログ無しで同じファイルへ書き戻せる＝目的の挙動そのもの。
  ・ハンドルは IndexedDB に保存し、キーは docId（保存データへ埋め込む一意ID）。ファイル名をキーにすると同名の
    別図と衝突するため。docId は buildSaveHtml で未採番なら発行し、状態JSONに載せて保存＝開き直した時に
    自分の保存先を引ける。旧版で保存したファイルは docId が無いので初回だけ保存先を尋ねる（後方互換）。
  ・「名前をつけて保存」は showSaveFilePicker に切り替えた。保存先を選べるようになるうえ、ここで選んだファイルが
    そのまま以後の上書き先になる＝上書き保存の入口を兼ねられる。id:'bdm-save' 指定でブラウザが前回のフォルダを
    覚えるため、実質「前回と同じ場所」が既定で開く。名前入力モーダルは残した（図の名称 S_diagramName を決める
    役割があり、ガント見出しの工事名等に使われるため）。
  ・別ファイルとして保存したときは docId を振り直す。振り直さないと、複製元と複製先が同じIDを持ち、複製先で
    上書きすると複製元を壊す。
  ・記憶していたハンドルを使う前に、中身の docId と（file:// で開いた場合は）ファイル名を照合する。エクスプローラーで
    ファイルを複製すると docId も複製されるため、ID だけでは複製先を開いて上書きした時に複製元を壊しうる。
  ・requestPermission はユーザー操作の直後にしか出せないので、許可の確認をハンドル復元の直後に置き、重い照合
    （ファイルを読んで docId を突き合わせる）は許可を取った後に回した。
  ・Ctrl+S は Ctrl+F と同じく INPUT ガードより前で処理＝入力欄にフォーカスがあっても効く。
  ・生成（buildSaveHtml）と書き出し（ダウンロード／ハンドル書き込み）を分離。非対応ブラウザ・ダイアログ失敗時は
    従来の saveHtmlCore（ダウンロード）へフォールバックするので、既存の動作は失われない。

■ 検証（how tested）
  ・書き込み経路は、ピッカーと同じ FileSystemFileHandle をOPFSから取得して端から端まで実行＝1.4MBの保存HTMLが
    書かれ、docIdを含み、読み直すと状態が完全に復元されることを確認。保存HTMLを iframe で実際に起動させ、
    ノード2件・図種・図の名称・docId・新ボタンまで復元されることも確認した。
  ・「開き直してから上書き」を再現（メモリ上のハンドルを捨てる）→ IndexedDB から復元し、無確認で同じファイルへ
    追記が反映されることを確認。
  ・安全装置＝記憶していた保存先が別の図のファイルだった場合、上書きせず保存先の指定に切り替わり、相手の
    ファイルが無傷であることを確認。
  ・Ctrl+S（キャンバス上／入力欄フォーカス中の両方）で発火、従来のダウンロード保存も正常、コンソールエラー0。

■ 既知の制約
  ・Microsoft Edge / Google Chrome 専用。Firefox・Safari は File System Access API 非対応のため、従来どおり
    ダウンロード保存になる（その旨をヒント表示）。
  ・.html をダブルクリック（file://）で開く運用で最後まで通るかは実機確認が必要。開発環境（Claude内蔵ブラウザ）は
    OSのファイルダイアログを描画しないため、file:// でピッカー呼び出しが拒否されないところまでは確認済みだが、
    実際の選択→書き込みは未確認。通らない場合はダウンロード保存へフォールバックする（壊れはしない）。
  ・同名ファイルを別フォルダへ複製した場合は、docId もファイル名も一致するため区別できない。
  ・セッションの最初の上書き時に、ブラウザの「ファイルの編集を許可しますか」が1回出ることがある（仕様）。

── v8.09 追補（2026-08-04）マインドマップの枝の形を切替式に（曲線／直線） ──

■ なぜ（why）
  ・「枝がたまに意図せず交差する」というユーザー指摘から経路設計を調査した。実測の結果、
    普通に自動整列した図（12ノード）では現行の描き方で交差0・貫通0であり、現行の描き方自体に
    問題は無かった。交差が出るのは、兄弟を手でほぼ同じ方向へ動かしたときに限られる。
  ・原因: 兄弟の枝が親の辺の中央という同じ1点から同じ向きに出るうえ、膨らみが枝の長さに比例する
    （mindBezier の off=max(30,dist*0.4)）ため、長い枝が短い枝を追い越す。加えて出口の辺が
    |dx| と |dy| の大小だけで決まるため、45度をまたぐと right→bottom に突然切り替わる。
    実測: 中心から43度と56度にある2本が、箱が重なっていないのに交差した。

■ 却下した案（why not）
  ・最初に「幹＋放射」案（親ごとに子の平均方向へ合流点を置き、そこから放射する）を提案したが、
    ユーザーから「現行の方が良く見える／改善案は歪」と指摘され取り下げた。
    放射状の図では子が四方に散るため平均方向がほぼ打ち消し合い、合流点が無意味な向きへ飛び出す。
    結果、右にある子への枝が左上から回り込む不自然な形になっていた。片側にだけ子が並ぶツリー表示を
    前提にした設計で、放射状の検証が不足していた。
  ・出口だけを輪郭上にする案・制御点だけ方向沿いにする案も試したが、別条件で交差が増えたため不採用
    （5本の兄弟で交差0→1）。

■ 設計判断（why this way）
  ・両端とも「相手の方向」の輪郭上の点を結ぶ形（＝直線）は、枝が自分の角度の中に収まるため順序が
    保たれ、追い越しが起きない。実測でも上記の交差1件が0件になり、普通の図では交差0・貫通0を維持した。
  ・ただし見た目が直線的に変わるため、置き換えではなく<切替>にした（ユーザー判断 2026-08-04）。
    既定は曲線＝従来のまま。切替はサイドバー「レイアウト」に置いた（配置モードの隣＝図全体の見え方を
    決める設定がまとまる位置）。
  ・輪郭上の点は既存の _edgeBorderToward（手動ウェイポイント経路の端点算出・v4.26）を再利用した。
  ・表示の切替なので snap() は呼ばない。呼ぶと履歴に中身の変わらない段が積まれ、Ctrl+Z が空振りする
    （方眼 toggleGrid・ステップ番号 toggleStepNums と同じ扱いに揃えた）。
  ・属性セグメントの一括制御は '.mind-seg' 全体を対象にしていたため、枝の形セグメントまで
    「ノード未選択なら無効」にされてしまう。セレクタを '.mind-seg[data-key]' に限定して回避した。

■ 検証（how tested）
  ・実クリックで切替が動作し、枝が全て直線（M..L..）／全て曲線（M..C..）に切り替わることを確認。
  ・兄弟を手でほぼ同じ方向へ置いた状態で、曲線=交差1 / 直線=交差0 を実測（描画済みSVGを60分割して判定）。
  ・保存→インポートで枝の形が復元されること、mindBranch を持たない旧ファイルは曲線になることを確認。
  ・切替を3回行っても履歴が増えないこと、属性セグメントの有効/無効・選択表示に回帰がないこと、
    補足情報の細点線と深さ別の太さが直線モードでも保たれること、出力SVGにも直線が載ることを確認。
  ・コンソールエラー0。

■ 残っている課題（未着手）
  ・枝が無関係なノードを突き抜ける（貫通）現象は、描き方をどう変えても直らない。ノードが枝の通り道に
    置かれているため。以下の配置側の改修が必要:
    ①Tabの子追加位置が「親の右へ70px」固定で、親が中心より左だと中心へ戻る（実測で貫通2件）。
     中心から外向きに置けば0件になることは確認済み。
    ②放射状レイアウトが角度を葉の数だけで配り半径を深さ×185px固定にしているため、箱の幅ぶんの
     角度が確保されていない。
    ③ツリーの兄弟の並び順がエッジ追加順で、ドラッグで並べ替えても整列すると元に戻る
     （樹状図 orgTreeLayout は現在位置順にソート済み＝同じ考え方を持ち込める）。
