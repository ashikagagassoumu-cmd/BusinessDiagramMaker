================================================================================
  ビジネスダイアグラムメーカー 開発記録 v8（why／経緯）
================================================================================

  本ファイルは v8 系の「なぜ・経緯・原因と対策・既知の注意」を記録する。
  各版で「何を変えたか（what）」は git のコミットメッセージを参照。
  索引・運用は 開発記録.txt／運用ルール.txt、計画は 開発ロードマップ.txt。

  ──────────────────────────────────────────────────────────────────────────────
  v8.01（2026-07-01）複数選択・水平垂直ドラッグ・レーン色・デザインテンプレート・おしゃれステップ図
  ──────────────────────────────────────────────────────────────────────────────

  背景・目的:
    ①選択とドラッグの操作性を一般的な作図ツール（PowerPoint 等）に合わせたい、②レーン色を
    後から手軽に変えたい、③図の「色は変えずに見た目（角丸・線・影）だけ」を一括で整えたい、
    ④画像で示された番号サークル/矢羽/ピルのようなインフォグラフィック調のステップ図を作りたい、
    というユーザー要望。①〜③は明確なので即実装、④は見た目主体のため事前にHTML/SVGモックで
    複数案を提示し、採用テンプレを決めてから実装した。

  設計上の判断（落とし穴と対策）:
    ・修飾キーの役割を「押した瞬間」に確定させない。従来は Ctrl+mousedown で即複製していたため、
      Ctrl+クリック＝複数選択と両立できなかった。対策＝mousedown では通常ドラッグを仕込むだけにし、
      「クリック確定(mouseup・未移動)＝選択トグル」「ドラッグ確定(mousemove・4px超)＝Ctrlでコピー化
      (convertDragToCopy)」に振り分けた。convertDragToCopy を必ず S_didDrag 成立後に呼ぶ点が肝
      （指の微揺れで誤コピーしないため）。Ctrl/Shift どちらのクリックでも複数選択できる。
    ・Shift+ドラッグの水平/垂直固定は、mousemove 冒頭で pt を制約（変位の大きい軸だけ残す）すると
      下流の全分岐（group / 自由配置 / レーン）に一括で効く。ただし整列・グリッド・レーン内吸着が
      固定軸を動かしてしまうので、S.drag.constrained のとき該当軸のスナップを無効化して軸を純粋に
      固定する。
    ・デザインテンプレート（S.style / STYLES）は「色（各シェイプの fill/stroke・レーンの bg/hd）は
      一切変えず、形の装飾（角丸rx倍率・線幅・影フィルタ・アクセント帯・矢羽/ピル形状）だけ」を
      差し替える方針。shapeEl の rx を styRx()、線幅を stySW() 経由にし、影とアクセント帯は
      renderNodes で shapeEl の戻り要素へ後付け（テキストに影を掛けない）。矢羽/ピルは基本の
      「プロセス(rect)」の形のみ差し替え（他 rect 系は角丸だけ変化）＝ガント等を壊さないため。
      影は SVG フィルタ（ds-*）を defs に静的定義。保存/復元に S.style・S.stepNums を追加。
    ・おしゃれステップ図: 番号サークルは新シェイプ step_num（円＋大きな01/02/03＋下にラベル）。
      番号は「読み順（横広がりなら左→右／縦広がりなら上→下）」で毎描画時に自動採番（stepOrder）
      するため、並べ替え・追加で番号が自動で振り直る。番号バッジ(S.stepNums)は一般のプロセス系
      シェイプ左上にも同ロジックで付与。配色テーマは作成済みシェイプへ色を上書きする明示操作
      （Ctrl+Z で戻せる）＝既定方針「色は変えない」の任意的な例外として提供。レイアウト挿入は
      既存の addNode/addEdge 相当で編集可能なノード列＋接続線を生成（挿入後もドラッグ/文字編集可）。

  既知の注意:
    ・デザインテンプレートと配色テーマは図全体（全図種）に効く（S.style はグローバル）。矢羽テンプレは
      「プロセス(rect)」のみ形が変わる。step_num は自由配置シェイプ扱い（レーンに拘束されない）。

  追補（v8.01 同セッション・版番据置）:
    ・レイアウト挿入を縦横モード追従に変更。挿入時 isV() を見て、縦モード＝上→下の列／横モード＝
      左→右の行で並べる。挿入後に向きを切り替えても toggleOrientation の全ノード x↔y 入替で列ごと
      回転する（番号は stepOrder が読み順を再判定して追従、番号サークルのラベルは縦並び＝円の右／
      横並び＝円の下へ _flowDirs で自動切替）。
    ・ステップ数を可変に。挿入した列を flowId で1グループとして管理し、列末尾に＋（flowAddStep＝末尾へ
      1つ追加・並び方向を継続）／－（flowRemoveStep＝末尾を1つ削除・最低1つは残す）ボタンを描画
      （renderFlowControls）。ボタンは選択モード時のみ表示し、class="flow-ctrl" を付けて
      buildExportSvg のクローンから除去＝SVG/PNG/PDF には出さない。


  背景・目的:
    建設・土木の施工工程表（バーチャート式）は、用紙上部に 工事名・工事場所・発注者・
    施工者・工期・担当（現場代理人等）・作成日・版数 を載せ、右上に承認（作成・確認・承認の
    押印）欄、下部に凡例を置くのが定番。これまでガントは時間軸ヘッダー(2段)しか無く実務の
    工程表として提出しづらかった。Web調査で項目名・配置の定番を確定した上で実装。現場ごとに
    記載項目が異なるため「自由な行（ラベル：値）の追加・編集・並べ替え」でカスタマイズ可能に。

  設計上の最重要判断（落とし穴と対策）:
    画面の当たり判定は全箇所 (clientY - rect.top)/zoomLevel のピクセル換算で、SVGの viewBox
    オフセットを考慮していない（例: クリック無視ガード if(py<GANTT_HEADER_H)、ganttSlotIndexAtY）。
    そのため「エクスポートで既に使っている負Y方向 viewBox 帯」を画面にも使うとクリック位置が
    全部ズレる。
    → 採用方式: 見出しを “上部の非操作ストリップ（正Y空間 y:0..ganttHeadH()）” として描き、
      時間軸ヘッダー・グリッド・行を見出し高さぶん下げる。下げ量を ganttBodyTop()
      （= ganttHeadH() + GANTT_HEADER_H）に集約し、「内容の上端＝GANTT_HEADER_H」だった参照を
      これに一括置換。時間軸ヘッダー(renderGanttHeader)は内部座標を一切変えず translate グループで
      平行移動（DOMイベント＋svgPt はそのまま効くので＋ボタン・列幅リサイズも不変）。
      これらをライブSVGへ描くので buildExportSvg のクローンにそのまま乗り、SVG/PDF出力が自動追従。
      画面の座標系は従来どおり「ユーザー単位＝ピクセル・原点0」を維持＝当たり判定は上端定数の
      差し替えのみで済む。

  レイアウト確定（モック反復でユーザーと合意）:
    工事名（大）＋情報を左に2列・下詰め（本体との余白を詰める）／右上に作成日＋承認欄（小さめ・
    画面の並びは左→右で承認・確認・作成＝紙面では右から作成・確認・承認）／凡例は「押印欄の下・
    右側ゾーン内」で横長（3列×2段）・角丸の黒枠で囲む（説明欄の下には侵入させない）。
    見出しの高さは行数・承認ON/OFF・凡例項目数で自動可変（ganttHeadModel が縦寸法のみ算出、
    横位置は描画時に SVG_W から右端基準で決定）。

  データモデル / 永続化 / 後方互換:
    S.gantt.header = {show,title,rows:[{label,value}],showDate,approval:{show,slots:[承認,確認,作成]}}
    S.gantt.legend = {show,auto,overrides:{key→ラベル上書き},extra:[{label,color,stroke}]}
    approval.slots は「描画順＝左→右」で格納（紙面で右から作成のため、表示時に右寄せ配置）。
    保存は saveHtmlCore が S.gantt 全体を JSON 化するので追加フィールドも自動で永続化される。
    旧ファイル（header/legend 無し）は描画前に ganttEnsureHeader() が既定補完＝そのまま開ける。

  凡例の自動生成:
    ganttLegendItems() が S.nodes を走査し、使われている要素だけを拾う＝タスク（予定）／進捗
    （progress>0 のとき）／マイルストーン／フェーズ／休工（suspensions 有り）／土日。色見本は
    SD.gantt_* の fill/stroke を参照（図と完全一致）。overrides でラベルだけ差し替え、extra で
    「電気設備（協力会社）」等の工種・業者色分けを手動追加できる。

  エクスポート反映:
    ・SVG/PNG: ライブSVGに描くのでクローンに自動で乗る。見出し表示時は buildExportSvg の図面名
      タイトル帯を省略（工事名と二重化しないため）。
    ・PDF: 表題＋時間軸ヘッダーを <thead> に内包し各ページ先頭で繰り返す（viewBox を 0..ganttBodyTop()
      に拡張）。本体はフェーズ境界で改ページ（従来どおり）。図面名帯は見出し表示時は出さない。
    ・Excel: 帯(BAND)を「1 + 見出し情報行数」に拡張し、行1に工事名＋作成日、続く行に情報2列、
      （承認ONなら）承認欄をテキストで展開。最下部に凡例（色塗りセル＋ラベル）を1行追加。
      freeze/printTitlesRow は時間軸のみ繰り返す既存挙動を踏襲（情報帯は1ページ目先頭のみ）。

  既知の注意:
    ・見出し表示時は calcCanvasSize で SVG_W を最低 1120px 確保（狭い図でも右ゾーン＝承認欄＋凡例が
      重ならないように）。
    ・設定UIの動的リスト（行・承認・凡例）はテキスト入力にフォーカス中は作り直さない（ganttHeadEditing
      ガード）＝入力中のキャレット/フォーカスを維持しつつ SVG だけライブ更新。

  検証（Claude_Preview 実DOM ＋ node --check）:
    headH＝承認OFF:164／ON:176、行数で追従。bodyTop=232 でクリック→正しいタスクindex、
    header OFF で bodyTop=56（＝完全に従来挙動・回帰なし）。設定UI＝行の追加/削除/▲▼並べ替え・
    承認トグル・凡例ラベル上書き・工種色分け追加が即反映。保存→JSON→復元ラウンドトリップで
    header/legend 保持。旧形式（header/legend 無し）も補完して描画。buildExportSvg は工事名を内包し
    図面名帯の二重化なし（nameBand=0）。PDF/Excel いずれも例外0で生成（Excel 13.8KB 出力確認）。
    全インラインスクリプトの node --check ＝SYNTAX_OK。

  （diagram_v7.11.html をコピーして diagram_v8.00.html を新規作成。<title>／#app-title-bar／serve.js 既定
    配信先を v8.00 に更新。大変更＝整数部を v8 に。次の小変更は v8.01。）

  ──────────────────────────────────────────────────────────────────────────────
  v8.00 追補（2026-06-30）情報欄（概要列）の操作性改善＝列削除・項目名の表示/編集まわり
  ──────────────────────────────────────────────────────────────────────────────
    版番据置の追補（バグ修正＋微改善）。what はコミット参照。why／原因と対策のみ記す。

    ① 追加列を削除する手段が無かった → 列ヘッダー下端中央にホバー表示の削除アイコン（赤×）を
       追加。確認ダイアログ（showConfirm）で警告してから削除し、列構成と各タスクの cells を破棄。
       既定列 c0（概要/担当＝desc/assignee bind）は基幹のため対象外（誤渡しもガード）。
       アイコン位置はユーザー指示で「ヘッダー行の下端」に統一（＋列追加ボタンも下端へ）。

    ② バグ: 追加列のセルは「本文(値)が非空のときだけ描画」していたため、項目名(ラベル)だけ
       入力しても何も表示されなかった（ユーザー指摘＝編集しても出てこない）。
       → 描画条件を「本文が非空、または 追加列で項目名が非空」に変更。c0 は従来どおり本文が
         空なら既定ラベル(概要/担当)も非表示＝空タスクで「概要：/担当：」を並べない挙動を維持。

    ③ 項目名を「列単位（全タスク共通）」に変更（ユーザー選択＝B案）。従来は項目名をタスクごとに
       cell.l で保持していたため、列見出しとして不自然だった。col.r3Label/col.r4Label に集約し、
       ganttColLabel/ganttSetColLabel を新設、ganttCellLabel(n,…) は列ラベルを返す薄いラッパに。
       本文(値)は引き続きタスクごと（c0=desc/assignee, 追加列=cells[colId].v）。
       後方互換: 旧データ（タスク別 cell.l）は ganttMigrateColLabels() が列ラベルへ集約（最初の
       非空を採用・冪等）。読込・インポート・undo復元の各経路に挿入し既存ファイルの項目名を保全。

    検証（Claude_Preview 実DOM）: 削除＝ホバー表示/確認後に列とcells破棄・c0は非対象。表示＝
    項目名のみでも追加列は表示・c0空はラベル非表示。列単位＝1タスクの項目名変更が全タスクへ反映、
    本文はタスク別、旧データ移行で「工区」を列へ集約、undo/redoで列ラベル復元。コンソールエラー0。

================================================================================
【追補】v8.00（版番据置・バグ修正） 2026-06-29
--------------------------------------------------------------------------------
■ 左サイドバー（編集バー）にスクロールを追加
  why: 見出し・凡例パネルが縦に長く（工事場所等の情報行＋承認欄＋凡例）、画面が低いと
       下側のコントロール（承認欄・凡例設定など）が画面外に隠れて操作不能になっていた。
  how: #app-header（図の名称欄）は固定のまま、その下の全セクション（シェイプ〜レーン）を
       #sidebar-scroll でくくり、flex:1 + min-height:0 + overflow-y:auto で縦スクロール化。
       ヘッダーは常時見える＝図の名称をいつでも編集できる。

■ 見出しの「工事名」を図の名称に一本化（見出し専用の工事名入力を廃止）
  why: 「工事名」と「図の名称」を二重入力させていた。実質同じものなので二度手間・不整合の元。
  how: 見出しパネルの工事名 input（gh-title）を撤去。renderGanttTitleBlock と Excel 出力の
       工事名は exportDiagramName()（＝S_diagramName＝上部「図の名称」）を参照。図の名称欄の
       oninput を onDiagramNameInput() に変更し、ガント＋見出し表示中は即 render() で
       タイトルをライブ追従。h.title は不参照（旧保存データの title は無視・後方互換で実害なし）。
  検証: Claude_Preview 実DOMで、図の名称編集→見出しタイトルが即時更新／工事名入力欄が消滅／
        サイドバーが scrollHeight>clientHeight でスクロール可／console エラー0 を確認。
================================================================================

================================================================================
【追補】v8.00（版番据置・改善） 2026-06-30
--------------------------------------------------------------------------------
■ 見出し（工事名・工事場所など）の編集を「ダブルクリックで開くダイアログ」に変更
  why: 見出しの編集欄を左サイドバー（幅250px）に置いていたため、1行が「ラベル＋値＋×」で
       横に詰まり、長い住所・会社名が見切れて入力しづらかった（ユーザー指摘＝狭すぎる）。
       タスク詳細編集と同じ「対象をダブルクリック→ダイアログ」の作法に揃える方が一貫する。
  方式（ユーザー選択＝B案・モック合意済み）: 見出し領域を覆う透明ヒット矩形を
       renderGanttTitleBlock に追加し、ダブルクリックで #gantt-head-modal（幅560px）を開く。
       サイドバーからは入力リスト（情報行/承認スロット/凡例上書き）を撤去し、表示ON/OFF等の
       トグルと「見出しを編集…」ボタンだけ残す（ユーザー指示＝トグルのみ）。
  設計: データモデル（header.rows / approval.slots / legend）は不変＝PDF/Excel/SVG出力は無改修で
       追従。ダイアログは既存setter（ganttHeadSetRow等）を再利用しライブプレビュー。開いた時点の
       header/legend/図名称を _ghBackup に退避し、OK=snap()確定／閉じる(=Esc/背景)=破棄して復元。
       動的リストの作り直しはテキスト入力フォーカス中スキップ（ganttHeadDlgEditing）でキャレット保持。
       工事名はダイアログ内でも「図の名称」と双方向連動（ganttHeadDlgSetTitle）。
  検証: Claude_Preview 実DOMで、ダブルクリック/サイドバーボタンの両方で起動・工事名/行/承認/凡例の
       編集がSVGへ即反映・行の追加/削除/▲▼・承認/凡例トグルでスロット/上書き欄が増減・OK確定で
       snap・閉じるで全項目バックアップ復元・サイドバーはトグル5個＋編集ボタンのみ・console エラー0。
================================================================================

================================================================================
【v8.02】スイムレーン等の接続線を改良（ラベル線上化・○ホバー化・終点整形・段違い） 2026-07-01
--------------------------------------------------------------------------------
■ 発端（ユーザー指摘・4点）
  ① 接続線のラベル/編集欄が線上に出ない（端点の直線中点＝折れ線から浮く）。
  ② ラベル無し線の○（追加ボタン）が常時表示でうるさい。
  ③ 1つの相手に加えて2つ目へ繋ぐと、扇状分散で1本目の始点がずれてL字に曲がる（真下なのに斜行）。
  ④ 3つ目へ繋ぐと2本目と3本目の横線が同じ高さで重なる。

■ 仕様決定（AskUserQuestion で合意・モックで反復）
  ・始点(出口)は等間隔(扇状分散)のまま許容。終点は「一直線にできる線は一直線を優先(端が中央で
    なくても可)／できない線は辺の中央に着地」。相手シェイプごとに判定を変える。
  ・重なり回避は「曲がる高さ(横トラック)を枝ごとに段違い」。ただし素直に段違いにすると外側の枝が
    内側の枝の横線を突っ切る＝クロスが出る（ユーザーが不可）。
  ・角丸など線の描き方は現行維持（今回いじるのは経路の座標＝frac と段違い位置だけ）。

■ クロスを出さない段違いの原理（重要・why）
  出口辺を共有する“曲がる線”を、相手までの距離が【近い相手＝相手寄り(下段)／遠い相手＝始点寄り
  (上段)】のトラックに置く。こうすると各線の縦の降り口は自分のトラックまでしか下りず、遠い相手の
  長い横線は近い相手の短い横線より上を通り、互いの縦線が相手の横線を突っ切らない＝交差も重なりも
  出ない（源が箱幅より広く相手が横に散る配置でも成立）。逆順(遠い=下段)にすると必ず交差する。

■ 実装（diagram_v8.02.html）
  ・allocatePorts 末尾に段違いパスを追加（['swimlane','flowchart','bpmn'] のみ対象）。
    出口辺ごとにグループ化→各線の出口X/Yを算出→終点 entryFrac を直線化(範囲内なら exitに一致)／
    中央(0.5)に決定→曲がる線が2本以上なら距離順に近い→遠いで段違いトラック位置 autoMid を割当。
    相手に複数入線する辺・始点/終点ピン・ひし形・戻り線・同一辺Uターンは対象外(従来動作を保持)。
  ・renderEdges: 手動 midPos が無い線は autoMid を orthoRoute へ渡す(_amid)。ラベル/○の基準点
    mx,my を端点中点から polylineMid(wps)＝実際の折れ線の中央へ変更。○は未選択時 opacity:0 とし
    線(ヒットパス)/○自身の mouseenter で表示・mouseleave で非表示(選択中は常時表示)。
  ・orthoRoute/角丸(wpPath,18)は不変＝終点・段違いは frac と midPos の指定だけで実現。

■ 検証
  puppeteer で 1→3(3レーン)を構築し実DOMのパスdを確認：1本目=真下へ一直線、2本目=下段トラックで
  相手中央着地、3本目=上段トラックで相手中央着地、交差・重なり無し、角は Q(角丸)維持。○は未選択線
  opacity=0／選択線 opacity=1、ラベルは polylineMid で線上。console エラー0。
================================================================================

--------------------------------------------------------------------------------
【v8.02 追補】遠い相手への接続を「上辺入り」に揃える（始点=右辺は従来のまま） 2026-07-01
--------------------------------------------------------------------------------
■ 発端（ユーザー指摘）
  横に大きく離れた相手への線が、相手の「左辺」から入り、途中の箱の高さを走って貫通しうる。
  原因は入口辺 entrySide=facingSide(相手,始点) が、始点が「上」より「横」に離れていると“横向き”と
  判定して左右辺入りにするため（縦成分 |dy| >= |dx|*0.27 のしきい値）。
■ 決定（ユーザー指示）: 始点(出口)は従来通り右辺から出してよい。終点(入口)だけ上辺に揃える。
■ 実装（diagram_v8.02.html・allocatePorts）
  ・1st pass: scoped(swimlane/flowchart/bpmn)・非ピン・非ひし形・非戻り線で、相手が主軸方向に完全に
    離れている(縦モード=相手が始点の下辺より下/上辺より上、横モード=右/左)線は entrySide を主軸の辺
    (上/下辺、横モードは左/右辺)へ上書き。exitSide は facingSide のまま（右出しを維持）。
  ・段違いパス: 出口と入口の軸が「直交」する群（例 右出し→上辺入り）は、
      遠い相手ほど出口辺の外側(手前=上/左, frac小)から出す よう exitFrac を並べ替え＋入口は中央。
      これで各線の縦降口が相手の横線を突っ切らず交差ゼロ（近い相手=辺の下側から短く、遠い相手=上側
      から長く、の入れ子状）。midPos段違いは不要＝自然なL字。軸が平行な群は従来の直線化/中央化/段違い。
■ 検証
  横長6分岐(縦ギャップ小)を実DOMで確認：t1真下=直線、t2/t3=下出し中央着地(段違い)、t4/t5/t6=右辺
  出し(遠いほど上:y=50/40/30)→上辺中央着地。全線 上辺入り・交差ゼロ・箱の貫通なし・角丸維持。
  真横/Y重なりの相手・ピン・ひし形・戻り線は従来動作を保持。
================================================================================

--------------------------------------------------------------------------------
【v8.02 追補2】接続線の扇整形を「向き・役割」対称に一般化（下→上／多対1に対応） 2026-07-01
--------------------------------------------------------------------------------
■ 発端（ユーザー指摘）
  ① 追補1の入口整形が「上→下」だけで、下→上（始点が下・相手が上）では機能しない。
  ② 上→下でも「複数シェイプ→1シェイプ(終点が扇の中心)」だと、1対多を線対称にしたルートに
     ならない（多対1が未対応）。
■ 原因
  A) allocatePorts の扇整形/入口整形が「始点(exit)を hub」に固定＝多対1(終点hub)を扱えない。
  B) 直交群の並べ替え(遠い相手ほど外側)の向きが下向き決め打ち＝上向きで逆転し交差。
  C) 入口整形と扇整形が isReturnEdge(縦モードで上向き=戻り線)を除外していたため、下→上の流れ
     全体が「戻り線」とみなされ整形対象外になっていた（これが①の主因）。
■ 対処
  ・processFan(eds, role) に一般化：role='exit'(hub=始点)/'entry'(hub=終点)をアクセサで切替え、
    上下左右いずれの辺・どちらが扇の中心でも同じ手続きで整形。各辺を「始点側が扇(srcFan)なら
    exit、終点側が扇(tgtFan)なら entry」でグループ化し、多対1も1対多の鏡像として処理。
  ・直交群の並べ替えは脚の向き(leg=相手の主軸方向の符号)で分岐＝上向き/下向きどちらも交差回避。
  ・isReturnEdge 除外を撤去（整形対象に含める）。ブリッジ描画は renderEdges 側の別フラグ(isReturn)
    で独立に動くため、扇整形に含めても前方線を跨ぐ戻り線のブリッジは従来どおり描かれる。
    U字(exitSide===entrySide)・ひし形は引き続き対象外。真横/Y(横モードはX)重なりの相手も従来のまま。
■ 検証（実DOM・6分岐＋戻り線）
  A上→下 / B下→上 / C多対1・下 / D多対1・上 の4形をレンダリング：いずれも真正面の相手は一直線、
  他は主軸の辺(上/下)へ中央着地、近い=手前/遠い=奥の段違いで交差ゼロ・箱の貫通なし。多対1は1対多の
  線対称。前方チェーン＋戻り線(C→A)も右へ回り込み前方線を避けて描画・console エラー0。
================================================================================

--------------------------------------------------------------------------------
【v8.03】マインドマップ機能拡大（合意状態・属性・レベル/フォーカス・テキスト入出力） 2026-07-08
--------------------------------------------------------------------------------
■ 発端
  参考資料「マインドマップアプリ開発参考資料.md」（ユーザー提供）に基づく機能拡大。
  資料の結論=差別化の核は「考えを増やす」より「考えを検証・収束させる」機能（合意状態の
  可視化・メタデータ）であり、これらはサーバ不要＝単一HTML完結方針と相性が良いと判断。
  AI生成・共同編集・クラウド連携系は設計方針と合わないため対象外（ユーザー合意）。
  当初は4小版に分割予定だったが、ユーザー指示で4機能を v8.03 に一括実装。

■ 設計上の判断（why）
  ・ノード属性は optional プロパティ（status / importance / confidence / effort）。
    snap()/saveHtmlCore() はノードを丸ごと直列化するため、追加コードなしで保存・undo・
    後方互換（旧ファイル=未設定）が成立。マイグレータ不要。資料4-1章の「属性データ設計は
    後付け困難」への回答として、まずデータ土台を作り全機能を同じ土台に載せた。
  ・バッジ類（状態=右上円 / 重要度=左縦バー / 確信度=枠線の線種 / 工数=右下ピル）は
    属性スタイル（fill/stroke直書き）で描く＝buildExportSvg はライブSVGの clone なので
    PNG/SVG/印刷へ無改修で反映される。実装前にHTMLモックで見た目をユーザー承認済み。
  ・減光（未決強調・属性絞り込み・フォーカス）は CSS クラス .mind-dim 方式。書き出しSVGには
    ページCSSが載らないため「画面では減光・出力はフル」が自動成立。防護として clone から
    クラス除去も追加。3機能は mindDimSet() の積集合に一本化。
  ・強調/絞り込み/フォーカスは実行時のみの表示状態（snap対象外・図種切替でリセット）＝
    undo履歴を汚さず、保存ファイルにも残さない。
  ・レベル切替は既存 collapsed の一括設定（Space折り畳みと同一機構）。undo可・保存に載る。
  ・FreeMind(.mm)書き出しは独自属性 *_BDM で完全往復＋組込アイコン（button_ok/help/
    messagebox_warning/full-1..3）併記＝FreeMind上でも状態・重要度が見える。
  ・取り込みは共通ビルダー mindBuildFromItems を新設し generateFromOutline 本体は不変更
    （既存アウトライン取り込みへの回帰リスク回避）。不正XMLは DOMParser の parsererror
    検出で {ok:false,error} を返し状態不変。

■ 落とし穴と対策
  ・サイドバーのラベルCSSがガント専用セレクタ（#gantt-settings label）だったため、
    #mind-settings 新設欄のラベルが崩れた。→ セレクタに #mind-settings を追加。さらに
    チェックボックス行は ID セレクタの column 指定が .gchk(row) に勝つため
    #mind-settings label.gchk{flex-direction:row} を明示。
  ・タイトルバー/<title> の版表記が v8.01 のまま残っていた（v8.02 更新漏れ）→ v8.03 で是正。

■ 既知の注意
  ・Markdown書き出しの【タグ】（状態・属性）は、アウトライン取り込みでは文字列として
    扱われる（属性の往復には OPML / .mm を使う）。
  ・テキスト書き出しは折り畳み中の枝も含む全ノードが対象（モーダルに注記あり）。
  ・減光(.mind-dim)は画面表示専用。出力に減光を残したい場合は将来オプション化する。
================================================================================
================================================================================
【v8.04】マインドマップのシェイプ拡充（問題/対策/補足情報）＋重要度表現改良＋凡例 2026-07-08

■ 発端
  ユーザー要望4点: ①シェイプは「トピック」1種のみ→「問題」（改称）＋「対策」「補足情報」を
  新設したい ②凡例を表示したい ③重要度は左バーでなく輪郭内側の二重赤線（太さで高中低）に
  したい ④重要度=高はシェイプ自体・文字も大きく／初期編集エリアが狭いので拡大したい。

■ 仕様決定（HTMLモックで反復し合意）
  ・対策=矢印タグ型（案B・ユーザー選択。案A=カプセル型は不採用）。
  ・補足情報=付箋型・実線枠（案A）。案B=吹き出し型は「破線枠が v8.03 の確信度=中/低の
    枠線種と紛らわしい」ため不採用（重要な選定理由）。付箋は枝色に染まらず薄黄固定・
    文字ひと回り小・枝は細い点線＝「本筋でない注記」を形と線種の両方で区別する。
  ・重要度の赤内枠は低・中も赤のまま（太さのみで区別）とユーザー確認済み。

■ 設計上の判断（why）
  ・「トピック」→「問題」は type='mind_topic' のまま名称のみ変更＝既存データ完全互換。
    新シェイプは新 type（mind_action / mind_note）を追加し isMindShape を拡張。
  ・シェイプ描画は mindShapeEl(n,attrs,inset) に一本化。inset>0 で内側平行輪郭を返すため、
    重要度の二重赤線が3形状（角丸/矢印タグ/付箋）すべてに同じコードで載る。
  ・重要度=高の拡大は calcMindNodeSize 内で実施（毎render再計算されるため設定即反映・
    保存データはサイズを持たない＝互換影響なし）。文字は mindNodeFont() で +2px。
  ・凡例は renderMindLegend() で g-dividers に描画（ライブSVGの一部なので PNG/SVG/印刷へ
    無改修で反映）。ガント凡例と同思想で「図内で使用中のシェイプ・記号だけ」を自動表示。
  ・初期編集領域はマインドマップのみ 1600×1000（他図種は MIN_CANVAS=800 のまま＝回帰回避）。
    中心トピック初期位置も (360,260)→(725,476)=新領域の中央へ。
  ・OPML/.mm はシェイプ種別を独自属性（_shape / SHAPE_BDM）で完全往復。Markdown は
    【対策】【補足】タグを書き出し（読み戻しなし・従来どおり OPML/.mm を推奨）。

■ 既知の注意
  ・矢印タグ型の合意バッジ・工数ピルは先端を避けて MIND_ARROW_TIP(12px) 内側に描く。
  ・補足情報の付箋は選択枠・確信度の線種変更は輪郭に適用される（折れ角は装飾のまま）。
  ・serve.js の既定配信先が v8.03 更新漏れだった（v8.02 のまま）→ v8.04 で是正。
================================================================================
================================================================================
【v8.04 追補】マインドマップ：ダブルクリックでの名称編集が実マウスで成立しないバグ修正 2026-07-08

■ 症状
  マインドマップの各シェイプ（中心/問題/対策/補足情報）をダブルクリックしても
  名称編集エディタが開かない（実マウス環境で再現）。

■ 原因（why）
  mindNodeMD（mousedown）で即 render() ＋ mouseup でも未移動クリックで無条件 render()
  していたため、1回目のクリックで対象の <g> 要素がDOMごと作り替えられ、2回目のクリックが
  「別の新要素」に当たって dblclick イベントが成立しなかった。
  ガント行・汎用ノードで v7.05 追補時に対策済みだった既知パターンの適用漏れ（v8.03 の
  マインドマップ新設時に mindNodeMD を独自実装したため対策が入っていなかった）。

■ 対策（how）
  ガント/汎用ノードと同方式に統一（3点）:
  ①mousedown（mindNodeMD）で即 render しない ②mouseup の未移動クリックは選択描画を
  220ms 遅延（nodeClickTimer・ダブルクリックと排他） ③dblclick ハンドラ冒頭でタイマー解除。
  ドラッグ移動・再ペアレント・単クリック選択は回帰なしを確認済み。

■ その他
  ・serve.js を PORT 環境変数対応に（プレビューのポート競合時に自動割当できるように）。
================================================================================
【v8.04 追補②】マインドマップ：パレット連続配置時に起点（選択）が新シェイプへ移る挙動を変更 2026-07-08

■ 発端
  パレットから1つ配置すると選択（＝次の配置の起点）が新シェイプへ移ってしまい、
  同じシェイプから連続して枝分かれさせられない（ユーザー要望）。

■ 対策（1行・仕様変更）
  mindPlace の配置後選択を S.sel=[n.id]（新シェイプ）→ S.sel=[parent.id]（親のまま）に変更。
  連続配置で同じ親からどんどん分岐する。別の起点にしたい時はそのシェイプをクリックして
  から配置すればよい（従来どおり）。

■ 注意（トレードオフ）
  配置直後に属性ボタン（状態・重要度など）を押すと親に適用される（従来は新シェイプに
  適用された）。新シェイプへの属性設定は当該シェイプをクリック選択してから行う。
  Tabキーでの子追加（mindAddChild）は従来どおり新シェイプ選択＋名称編集を開く（変更なし）。
================================================================================
【v8.04 追補③】マインドマップ：シェイプ内テキストの改行対応（Alt+Enter） 2026-07-09

■ 発端
  シェイプ内のテキストを複数行にしたい（Alt+Enterで改行したい）というユーザー要望。
  版アップ不要の指示（小変更）。

■ 実装（why / how）
  ・エディタ: openSvgEditor に opts.multiline を追加（複数行エディタ強制）。Alt+Enter＝
    カーソル位置に改行挿入・Enter＝確定は従来どおり（既存の enterNewline 方式とは別軸で共存）。
    mindEditNode は常に multiline で開く（表示高さ最低46px）。
  ・描画: 複数行は <tspan> で行ごとに描画（ライブSVGの一部なので PNG/SVG/印刷へ無改修で反映）。
    1行のときは従来どおり textContent（デグレ回避）。
  ・サイズ: calcMindNodeSize を複数行対応（幅=最長行・高さ=基本高+行数×行送りfs*1.3）。
  ・出力の往復: xmlEsc に改行→&#10; を追加（XMLの属性値正規化で literal 改行はスペースに
    潰れるため。&#10; なら OPML/.mm の再取込で改行が復元される）。
    Markdown 出力は改行→スペース（アウトライン構造の破壊防止・MD取込はもともと非往復）。

■ 検証（ブラウザ実機）
  ・Alt+Enterで挿入→Enter確定→2行tspan表示・シェイプ高さ自動拡張（32→48等）を確認。
  ・全シェイプ（中心/問題/対策/補足情報）で複数行表示・1行ノードは従来描画のまま。
  ・OPML/.mm 出力に &#10; が入り、DOMParser 再解析で改行復元・XML妥当性OK。
