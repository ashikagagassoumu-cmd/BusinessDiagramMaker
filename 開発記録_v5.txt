================================================================================
  ビジネスダイアグラムメーカー 開発記録（変更履歴）
================================================================================

  本ファイルはバージョンごとの変更内容を記録する。
  運用ルールは「運用ルール.txt」、開発計画は「開発ロードマップ.txt」を参照。
================================================================================
  v5.xx 変更履歴（v5.00〜v5.03）
================================================================================
--------------------------------------------------------------------------------
  v5.03  2026-06-05
  機能 — ガント週/月表示: 完了日までの期間が短いとき、列幅を自動拡大して横幅を画面いっぱいに
            （ユーザー要望）:
            ・週/月表示で、開始日～完了日(endDate)の単位数が画面幅に満たない場合、列幅を
              「(表示領域幅−左欄−余白)÷期間単位数」まで拡大し、工程表が画面いっぱいになる。
            ・期間が長い場合はスケール既定幅（週48/月80）を下限とし、既定より狭くしない。
            ・日表示・完了日未設定は対象外（従来どおり）。
            ・ズーム値 g.colWidth は据え置き、描画用の実効列幅 ganttColW()（renderGantt冒頭で
              キャッシュ）を別途算出し、日付↔X・列数・グリッド・ヘッダー・ドラッグ・キャンバス
              幅に適用。月既定(80)＝旧最大(80)のため、実効幅方式で上限を超えて拡大できるように。
  検証: Claude_Preview で 週/月×短期間=拡大し画面フィット／長期間=既定維持／日表示=不変／
        日付往復変換の整合／コンソールエラー無し を確認。
  （v5.02 をコピーして diagram_v5.03.html を新規作成。<title>／#app-title-bar を v5.03 に、
    serve.js 既定配信先も v5.03 に更新。v5.02 からは本機能を巻き戻し、「スイムレーン置換＋
    横線修正」までに戻した。版分離はユーザー選択。）
  変更ファイル: diagram_v5.03.html（新規）, diagram_v5.02.html（巻き戻し）, serve.js,
                運用ルール.txt, 開発記録.txt, 開発ロードマップ.txt
  追補（2026-06-05・微修正・版番据置・ユーザー要望で直接修正）: ガント情報欄の末尾「空欄行」を
        「空のタスク行」と同じ見た目に統一。(1)空欄行の高さをタスク行既定(88px)に合わせる
        （ganttEmptyRowHeight＝GANTT_ROW_PAD*2＋GANTT_CELL_TOP_GAP＋GANTT_LINE_H*4）／(2)空欄
        行にも内部区切り（工程↔概要の横線＋列の縦区切り線）を描画（文字は無し）。チャートは
        空欄行ぶん縦に伸びる。空欄行数は従来式のまま。Claude_Preview で1列/2列とも確認・エラー無し。
  追補2（2026-06-05・微修正・版番据置・ユーザー要望で直接修正）: ガントのタスクバーの太さ
        （高さ）を20%太く。GANTT_BAR_H.gantt_task を 25.2→30.24 に変更（◆マイルストーン20／
        フェーズ21.6 は不変）。バーは行帯の中央配置のまま。Claude_Preview で確認・エラー無し。
--------------------------------------------------------------------------------
  v5.02  2026-06-05
  UX — スイムレーン図: 既存シェイプに重ねて別シェイプを配置すると「種類の置き換え」に
            （ユーザー要望）:
            ・パレットからのクリック配置／ドラッグ&ドロップのどちらでも、配置先に既存
              シェイプがある場合は新規追加せず、その既存ノードの種類を置き換える。
            ・テキスト（ラベル）は保持。サイズは新しい種類の既定に変更し、中心位置は維持。
            ・置き換え対象に重なっている間はオレンジの破線枠で強調表示（置換の合図）。
            ・対象はスイムレーン図のみ。crosslane（複数レーン横断）と同一種類は対象外
              （＝従来どおり新規配置）。他図種の挙動は不変。
  実装: nodeAtPoint を流用した replaceTargetAt / replaceNodeType / showReplaceHL /
        clearReplaceHL を追加。placeShape 冒頭で置換判定し、両配置経路（svgMD の activePal
        経路・palDragStart のドロップ経路）とホバー処理に強調表示を組込み。SVG に
        g-replace-hl レイヤーを追加。Claude_Preview で両経路・除外条件・強調表示を検証済み。
  （v5.01 をコピーして diagram_v5.02.html を新規作成。<title>／#app-title-bar を v5.02 に、
    serve.js 既定配信先も v5.02 に更新）
  変更ファイル: diagram_v5.02.html（新規）, serve.js, 運用ルール.txt, 開発記録.txt, 開発ロードマップ.txt
  追補（2026-06-05・微修正/版番据置・ユーザー要望でバージョンアップせず直接修正）:
        ガント横区切り線の欠落を修正。末尾フィル行（最終スロット下端〜SVG下端の余白行）に
        横線が引かれておらず「最終行の手前の行だけ線が無い」状態だった。原因＝行区切り線は
        L.slots ぶんしか描かず、renderGanttRows の末尾フィル（while y<SVG_H）に追従していな
        かったこと。対策＝行境界Yを ganttRowBoundaries()（スロット上端＋フィル行上端）に
        一元化し、工程側(renderGanttGrid)・情報側(renderGanttNameCol)の両方をこれに統一。
        Claude_Preview で 588 の線追加・多タスク時の回帰なし・コンソールエラー無しを確認。
--------------------------------------------------------------------------------
  v5.01  2026-06-04
  バグ修正/UX — ガント デバッグ3点（ユーザー要望・v5.00 第一段階の追補）:
            (1) 時間軸の縦グリッド線の色がバラバラ→一様に統一
            (2) 編集モーダルに追加列（c0以外）の項目名・本文入力欄を追加
            (3) 情報欄（名称・各セル）のダブルクリックでも編集モーダルを表示
  （v5.00 をコピーして diagram_v5.01.html を新規作成）
--------------------------------------------------------------------------------

  【指示内容（ユーザー 2026-06-04）】
    1. ガントの縦軸（時間軸）の線の色がバラバラでデザインがおかしい→一様に。
       さらにスクリーンショットを見ての追補指摘:
       ・列の境目の線が途切れて見えて違和感がある。
       ・工程と概要の間の行間にも横方向の線を描いてほしい。タスクの内側の線は細く。
       ・タスクの境目の横線が情報欄側と工程表側で太さが違うので同じに。
    2. 列追加後、タスクシェイプをダブルクリックで開く編集画面に、挿入した列の
       項目名・本文を入力する欄が無い→追加する。
    3. 情報欄（タスク名称等の列）をダブルクリックした場合も編集画面を表示する。

  【実装内容】

    ■ 版管理
      ・diagram_v5.00.html をコピーして diagram_v5.01.html を新規作成。
        <title>／#app-title-bar を v5.01 に、serve.js 既定配信先も v5.01 に更新。

    ■ 1. ガントの線を整理（縦線一様化＋横線の追加・統一）。線を2階層に整理:
        「境界(major)＝#cbd5e1/1」「内側・グリッド(minor)＝細線」。
      ・(1) 時間軸の縦グリッド線を一様化（renderGanttGrid）。従来は月初を #cbd5e1/1、
        他を #e8edf2/.6 と色・太さで区別していたのを、全て #e8edf2/.8 に統一。週表示で
        別途描いていた月初の太線も #e8edf2/.8 に揃え、色違いを解消（月境は上段ヘッダーの
        年月ラベルで識別）。検証: 日/週とも g-grid の縦線スタイルが #e8edf2/.8 の1種類のみ。
      ・(2) 列区切り線の途切れを解消（renderGanttNameCol）。従来は r3Top〜cellsBot のみで
        上下が他の線に接続せず浮いて見えた。上端＝工程↔概要線(r3Top)、下端＝タスク境目線
        （スロット下端 top+s.height）まで延長し、両端を横線に接続。細線 #cbd5e1/.6。
        検証: t1 の列区切りが x一定・y=r3Top(137)〜slotBot(178) で連続。
      ・(3) 工程行と概要(セル)行の間に横の区切り線を追加（renderGanttNameCol）。
        bx→leftW に #cbd5e1/.6 の細線。これがセル列区切りの上端の接続先にもなる。
        「タスクの内側の線（工程↔概要・列区切り）」は細線(.6)、タスク境目は太線(1)で区別。
        検証: y=r3Top の横線（x=84→484）を確認。
      ・(4) タスク境目の横線を情報欄側・工程表側で同一に。情報欄側(renderGanttNameCol)を
        従来 #cbd5e1/.8 → #cbd5e1/1 に変更し、工程表側(renderGanttGrid)にも各スロット上端へ
        leftW→SVG_W の #cbd5e1/1 横線を新規描画（bars より背面の g-grid）。両側で太さ一致。
        検証: g-grid 横線 #cbd5e1/1（x=484→2436）が9本、情報欄側も #cbd5e1/1 で同値。

    ■ 2. 編集モーダルに追加列の項目名・本文欄（openGanttEditor／confirmGanttEdit）
      ・openGanttEditor: タスク/マイルストーン（!isSum）の概要・担当者の下に、ganttColList()
        の c0 以外の各列について「追加列 N（項目名：本文）」欄を生成。行3・行4それぞれに
        項目名(gm-col-{colId}-{r3|r4}-l)＋本文(gm-col-…-v)の2入力を flex で横並び配置。
        値は ganttCellLabel/ganttCellVal で初期化（既存セル内容をプリフィル）。
      ・confirmGanttEdit: フェーズ(summary)以外で c0 以外の各列・r3/r4 の入力を読み、
        ganttSetCell(n,colId,row,label,val) で保存（c0 は従来どおり概要/担当に bind）。
      ・検証: 追加列1列の状態で t1 のモーダルに gm-col-gc6-r3/r4 の項目名・本文4欄が出現し
        「発注者/△△市・用途/住宅」をプリフィル。値を書換え→適用で node.cells.gc6 に反映
        （発注者:◯◯市・区分:商業）、desc も更新、モーダル閉。スクリーンショットで欄の表示を確認。
      ・補足: c0（既定列＝概要/担当）の項目名(ラベル)変更は本モーダルでは扱わない（値は
        概要/担当欄で編集。既存のカスタムラベルは confirmGanttEdit で上書きせず保持）。

    ■ 3. 情報欄ダブルクリックで編集モーダル（renderGanttNameCol の addHit）
      ・名称行・各セルの透明ヒット矩形の dblclick を ganttEditField/ganttEditCell（インライン
        編集）→ openGanttEditor(n.id) に変更し、工程バーのダブルクリックと同じ編集画面に統一。
        日付(工程)行は従来からモーダルを開く実装で変更なし。
      ・検証: 情報欄(名称行)上の点へ実 dblclick を発火→ gantt-modal が表示され _ganttEditId が
        当該タスク(t1)になることを確認。コンソール error=0。

    ■ 4. 行3（概要）の上側余白を確保（同版内・ユーザー要望／版据え置き）
      ・背景: v5.01 で工程↔概要の区切り線を追加した結果、3行目（概要）の上側の余白が小さく
        窮屈に見えるとの指摘。
      ・定数 GANTT_CELL_TOP_GAP=6 を新設。工程↔概要 区切り線(lineY=top+pad+lh*2)はそのままに、
        セル本文(行3・行4)の開始位置を GAP ぶん下げて区切り線と本文の間に余白を作る。
        ganttRowHeight に GAP を加算（行高 +6px）。renderGanttNameCol は lineY（線・列区切り上端）
        と r3Top=lineY+GAP（本文）を分離。インライン編集(ganttEditField 概要/担当・ganttEditCell)の
        y座標も GAP ぶん下げて描画と一致させた。
      ・検証: GANTT_CELL_TOP_GAP=6・行高 88、セルのヒット矩形 y が r3Top(=143) と一致、情報欄
        ダブルクリックでモーダル表示（回帰なし）、スクリーンショットで概要の上に余白を確認。
        コンソール error=0。

    ■ 5. フェーズ縦バンドの包括幅ドラッグ中の表示不具合を修正（同版内・ユーザー要望／版据え置き）
      ・背景: フェーズ縦バンドの下端ハンドルを上下ドラッグして包括幅を調整する際、調整中（プレビュー）
        だけ縦バンドの上端がフェーズ専用ヘッダー行を外れてメンバー先頭へ飛び、ヘッダー行ぶんの帯が
        消えて見えた（確定後は ganttPhaseSpan で正しく表示されるため、調整中のみ発生）。
      ・原因: renderGanttPhases のプレビュー分岐が span を {ganttTaskSlot(preview.top).top,
        ganttTaskSlot(preview.bot).bottom} とし、ヘッダー行スロットを含めていなかった（通常時の
        ganttPhaseSpan はヘッダー行 phase.id を含む）。
      ・修正: プレビュー span の top を、ヘッダー行スロット(ganttSlotOf(p.id))の top がメンバー先頭
        より上ならそちらを採用するよう変更し、調整中もヘッダー行を帯に含める。確定処理
        (ganttApplyPhaseRange)は変更なし。
      ・検証: 下端ドラッグで拡大(t3まで)・縮小(t1のみ)とも縦バンド上端が header行(y=59)に固定され、
        通常表示(band0)と一致。スクリーンショットでヘッダー行～メンバーを連続して覆うことを確認。
        コンソール error=0。

    ■ 6. マイルストーンのx配置: 日表示は日列の中央に（同版内・ユーザー要望／版据え置き）
      ・背景: v4.30 で全スケール一律 cx=ganttDateToX(date) としたが、日表示では ganttDateToX が
        「その日の列の左端」を返すため、マイルストーンの菱形が日列の左端（前日との境目寄り）に
        付いて見えた。日表示では「日付＝1日＝1列」なので、列の中央に置くのが自然というユーザー指摘。
      ・修正: calcGanttPositions のマイルストーン分岐で、日表示のみ cx=ganttDateToX(date)+colWidth/2
        （日列の中央）に。週/月は従来どおり cx=ganttDateToX(date)（列内の比例位置。v4.30 確定の挙動を
        維持＝「日表示は他と違う設定」というユーザー指摘どおり）。ドラッグはデルタ方式(du)で日付を
        更新するため描画オフセットと独立＝挙動不変。
      ・検証: 日 cx=544（=ganttDateToX 528＋colWidth/2 16）で 6/10 列の中央・同日単日タスクの中心と一致、
        週 cx=281.14／月 cx=249.6 はいずれも ganttDateToX と同値（差0＝従来どおり）。スクリーンショットで
        日表示の菱形が 6/10 列中央に来ることを確認。コンソール error=0。

  ※ 残課題（v5.00 から継続）: PDFページ番号「# / #」の扱い（要相談）。列の削除/並べ替え等は v5.02〜。

  【変更ファイル（この記録時点）】
    diagram_v5.01.html（新規）, serve.js, 運用ルール.txt, 開発記録.txt

--------------------------------------------------------------------------------
  v5.00  2026-06-03  ※第一段階・実装中（随時追記）
  大変更 — 工程表対応の汎用化（汎用カラムモデル土台）の第一段階に着手。
           本版は複数の改修を束ねる作業中バージョン。確定仕様は
           「開発ロードマップ.txt」v5.00 第一段階を参照。
  （v4.30 をコピーして diagram_v5.00.html を新規作成）
--------------------------------------------------------------------------------

  【第一段階の確定仕様（2026-06-03 ユーザーすり合わせ）】
    左情報欄を「行1＝タスク名称／行2＝工程＝全幅（列で割らない）」「行3・行4＝列ごとの
    自由セル（項目名：本文）」に作り替え、⊕で右に列追加（全タスク共通／中身は各タスク
    自由・直近の項目名を記憶）、列幅は境目ドラッグ（⇔）で調整・折返し＋ぶら下げ維持・
    自動再折返し。既定1列目＝概要/担当（既存データ引継ぎ・編集可）。加えて
    ①完了日／②右端はみ出し文字の右詰め／③情報欄クリック・ドロップ追加／
    ④選択タスク直下挿入。値の持ち方＝組込列は既存ノードフィールドに bind、新規列のみ
    node.cols。（詳細は開発ロードマップ.txt）

  ── 実装済み（この記録時点）─────────────────────────────────

    ■ 版管理
      ・diagram_v4.30.html をコピーして diagram_v5.00.html を新規作成。
        <title>／#app-title-bar を v5.00 に、serve.js 既定配信先も v5.00 に更新。

    ■ ① 完了日（表示範囲を開始日～完了日で区切る）
      ・defaultGantt に endDate:''（空＝従来どおり自動延長）を追加。
      ・ツールバー（#gantt-end）・サイドバー（#gantt-end-sb）に「完了日」入力を追加。
        ganttSetEnd(v) で設定し、updateGanttToolbar で両UIを同期。
      ・calcCanvasSize（ガント分岐）: endDate 指定時は maxUnit＝開始日～完了日の単位数で
        SVG_W を算出し右端を完了日に固定（タスクが先に伸びても延長しない）。endDate 空の
        ときは従来（visibleUnits＋タスク終端＋余白）。
      ・検証: 開始2026-06-01／完了2026-06-15で SVG幅 2200→720（6/1〜6/15の15列ぶん）、
        クリアで2200に復帰、ツールバー/サイドバー同期、コンソール error=0。

    ■ 週表示の日付線・ラベル修正（ユーザー指摘・最優先バグ）
      ・背景: 週表示で「太い縦線」が最初の週の月曜（例 7/6）に出ており、実際の月初(7/1)に
        無いため、人が太線＝月の境目と読むと日付がずれて見えた（シェイプの x 位置自体は
        ganttDateToX で正しく、ズレではない）。
      ・renderGanttGrid: 週表示の月曜線をすべて細線（#e8edf2/.6）に統一し、各月の1日に
        太線（#cbd5e1/幅1＝日表示の月境と同じ太さ・色）を ganttDateToX(月初) の実位置で
        別途描画。
      ・renderGanttHeader: 週表示を専用分岐に。下段は各週の「月曜日付（○日）」をその月曜線の
        中央に重ねて表示（月曜が1日なら「1日」も表示／左端のみ左欄ヘッダーに隠れぬよう線中央
        から右へ寄せる）。上段の年月は実際の月境(1日)で区切り、本体の太線と一致。日/月スケール
        の表示は変更なし。
      ・検証: 太線が 6/1・7/1・8/1 の月初位置に正確一致（ganttDateToX と一致）、細線＝月曜、
        下段ラベルが月曜線中央に整列（1日含む・左端は右寄せで全体表示）、コンソール error=0。

    ■ ② 完了日で区切ったときの右端はみ出し文字の右詰め（ユーザー要望）
      ・renderGanttTaskBar／renderGanttMilestone: S.gantt.endDate 指定時のみ、ラベルの想定右端が
        SVG_W を超える場合に text-anchor を end に切替え x=SVG_W-4 で右寄せ（バー/シェイプ位置・
        列幅・レイアウトは不変＝変えるのは文字だけ）。マイルストーンは中央揃え→右詰めに切替。
      ・検証: 完了日2026-06-15（SVG_W=720）で 6/14開始タスク／6/15マイルストーンの文字が
        anchor=end・x=716 で枠内に収まり、中央寄りのタスク/マイルストーンは従来どおり
        （start／middle）。スクリーンショットで右端マイルストーン名が枠内右詰め表示を確認。
        コンソール error=0。

    ■ ③ 情報欄へのクリック／ドロップでもタスク等を追加（ユーザー要望）
      ・ganttPlace の早期 return 条件から「px<ganttLeftW」を外し、ヘッダー行（py<GANTT_HEADER_H）
        のみスキップ。情報欄に置いた場合は時間軸 X が無いので、開始日は基準タスク（後述④）の
        日付、無ければ S.gantt.startDate を採用。
      ・検証: px=100（情報欄）でも ganttPlace でタスクが追加されること（4→5）を確認。

    ■ ④ 新規タスク等を左クリックしたタスクの直下に挿入（ユーザー要望）
      ・ganttPlace: 基準（anchor）＝選択中の行ノード（S.sel）、無ければドロップ位置の行
        （ganttSlotIndexAtY(py)）。新規 row を anchor.row+0.5 で割り込ませ、追加後に
        ganttNodesSorted を 0..N-1 へ再採番して確定。anchor が無ければ従来どおり最下行。
        直下挿入は anchor の phaseId を継承（同フェーズ・ブロック整合）。
      ・検証: t1(6/3)を選択して追加→新タスク(6/12)が row1（t1直下）に入り最下ではないこと、
        連続追加で直前タスクの直下に積まれることを確認。コンソール error=0。

    ■ PDF出力: 工程表ヘッダー（年月・日付＋列見出し）を全ページに繰り返し＋図面名/日付帯（ユーザー要望）
      ・背景: 従来は図面名帯・時間軸ヘッダーが先頭ページのみで、2ページ目以降は日付が分からず不便。
        改ページ後のページにも工程表ヘッダー（フェーズ/タスク列見出し＋年月・日付の時間軸）を出したい。
        図面名・日付・ヘッダーは出力倍率に追従させる。
      ・buildExportSvg(skipTitle) を追加（PDFはSVG内の旧タイトル帯を省く）。expPDF を作り替え:
        - 図クローン上部(負y)に「図面名(左・v4.30と同じ W*0.02→24〜48px)＋出力日 yyyy年m月d日(右・小さめ)」
          帯をSVGで描画（pdfTodayStr）。文字をSVG内に置くことで図面名・日付・工程表ヘッダーが倍率%に一緒に追従。
        - ガント: <table> の <thead> に「帯＋工程表ヘッダー(y=-bandH〜GANTT_HEADER_H)」のヘッダーSVGを置き、
          印刷時に各ページ先頭へ自動的に繰り返す（display:table-header-group）。各フェーズ（区間の行SVG）は
          <tbody> の <tr> に並べ、page-break-inside:avoid で「フェーズ途中では改ページせず、収まらないときだけ
          フェーズごと次ページへ」＝v4.30 と同じ自然改ページ（フェーズ毎の強制改ページはしない）。フェーズあり
          は ganttPageGroups でフェーズ単位、末尾の空き行は contentBottom で除外。
        - 倍率%・用紙サイズ自由選択（@page size 無し）は維持。非ガント図種は単一SVG＋帯。
      ・検証: window.open をスタブして生成HTMLを確認＝<thead>(table-header-group)あり・<tbody tr>に
        page-break-inside:avoid・page-break-before:always 無し・列見出し「タスク / 概要 / 担当」あり・
        図面名 font-size=46(W=2276)で v4.30 と同サイズ。コンソール error=0。
      ・既知の制約: 1フェーズが1ページに収まらないほど大きい場合のみ、ブラウザが途中分割する（best-effort）。
      ・ページ番号「# / #」は保留: 自然改ページ（収まらないとき改ページ）では HTML/CSS で物理ページ数を
        数えられない（Chrome印刷は @page カウンタ非対応）ため正確な番号付けは未実装。扱いは要相談
        （ブラウザ印刷の「ヘッダーとフッター」機能で出す等）。

    ■ 完了日の既定を月末に（ユーザー要望）
      ・defaultGantt の endDate を空（自動延長）→ ganttDefaultEnd(start)＝開始から約 visibleUnits 日に
        達する月の末日（キリ良く月末・旧「画面いっぱい」相当）に変更。例: 開始2026-06-01→完了2026-07-31。
        旧ファイルは保存値（endDate）を Object.assign で維持（空なら従来どおり自動延長）。
      ・検証: 新規ガントで S.gantt.endDate='2026-07-31'（開始6/1）を確認。コンソール error=0。

    ■ 列モデル: 左情報欄を「行1名称／行2工程＝全幅」「行3・4＝列ごとの自由セル」に作り替え（v5.00 中心機能）
      ・データモデル: S.gantt.cols=[{id,width}]（全タスク共通の列構成）。各タスクのセル内容は
        node.cells[colId]={r3:{l,v},r4:{l,v}}（l=項目名,v=本文）。既定列 c0 は値を desc/assignee に
        bind（旧ファイル・検索・CSV取込と整合）、項目名(ラベル)と追加列は node.cells に保存。
        ganttColList/ganttColsTotalW/ganttCellLabel/Val/SetCell/ganttCellLines/ganttRowMaxLines を新設。
        ganttLeftW=フェーズ列＋列幅合計。ganttRowHeight=pad*2＋lh*(2＋行3最大行数＋行4最大行数)。
        旧ファイルは cols 欠落時に c0 を nameW から合成（無損失。node.cells・cols は既存の保存/復元に自動追従）。
      ・描画(renderGanttNameCol): 行1名称・行2工程は全幅のまま。行3・4は列を走査し、各列の x..x+width に
        「項目名：本文」を折返し＋ぶら下げで描画（列幅に応じ自動再折返し）。列間に縦区切り線。
      ・⊕列追加(ganttAddColumn): 情報欄と時間軸の境目付近（ヘッダー行）に⊕ボタン。押すと右に1列追加
        （全タスク共通・既定幅160）。
      ・セル編集(ganttEditCell/openGanttCellEditor): 行3・4のセルをダブルクリックで「項目名＋本文」の
        2フィールド・インライン編集。c0 含め項目名も編集可。直近の項目名(S.gantt.lastLabel)を新規セルの
        初期値に。c0は値を desc/assignee に、追加列は node.cells に保存。
      ・列幅ドラッグ(mode:'colw'): 各列の右境界に col-resize ハンドル。ドラッグで当該列幅を変更（60〜700）。
        既定1列のときは nameW も同期。折返しは render で自動再計算。
      ・検証: ⊕で1→2列(leftW 240→400)、新列セル保存(発注者:△△市→node.cells)、c0項目名編集(概要→説明)、
        c0本文は desc にbind、2フィールドエディタ、列幅変更(leftW反映)、全10図種 描画OK・回帰なし・
        コンソール error=0。スクリーンショットで「名称/工程＝全幅、行3・4＝概要/担当｜発注者/用途の2列」を確認。
      ・追補(同セッション・ユーザー指摘): (1) タスク行/列の境目線を濃く統一（#eef2f6/.6 → #cbd5e1/.8。週末
        網掛けは対象外）。(2) 列幅ドラッグが効かない不具合を修正＝colw の mousedown で startY 未設定だったため
        Math.hypot(...,pt.y-undefined) が NaN になり moved 判定されず。startY:pt.y を追加。実機ドラッグ再現で
        列幅 240→330・leftW 反映を確認。

  ※ v5.00 第一段階の中心（列モデル＋①完了日／②右詰め／③クリック追加／④直下挿入＋PDF・完了日既定）は
    実装完了。残課題: PDFページ番号「# / #」の扱い（要相談）。列の削除/並べ替え等は v5.01〜。

  【変更ファイル（この記録時点）】
    diagram_v5.00.html（新規）, serve.js, 開発ロードマップ.txt, 開発記録.txt

