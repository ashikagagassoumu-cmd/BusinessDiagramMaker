# ビジネスダイアグラムメーカー — プロジェクト指示

ブラウザだけで動く、単一HTMLファイル完結のビジネス図作成ツール。最新版を直接更新していく運用。

## 開発継続の前に確認するファイル（全文を毎回読む必要はない）

新セッションで開発を継続する際は、まず以下の「索引＋現行ステータス」を確認すればよい。
詳細が必要なときだけ該当ファイルを開く。

- `開発記録.txt`（索引）→ 最新の変更履歴は `開発記録_v8.txt`（アクティブ）
- `開発ロードマップ.txt`（全体計画＋現行ステータス＋索引）
- `運用ルール.txt`（バージョニング・作業ルール）

## 現行ステータス

- 最新版: `diagram_v8.09.html`（2026-08-03／①重ね置き入れ替えをマインドマップ・フローチャートへ拡大、②上書き保存を新設。①パレットから既存シェイプに重ねて配置すると形状だけ入れ替わり文章は保持される既存機能（v5.02スイムレーン図・v7.06樹状図）を適用。マインドマップは問題/対策/補足情報を相互に入れ替え（文章・状態/重要度等の属性・枝の接続を保持／サイズは自動再計算）。中心トピックと、折りたたみ・絞り込みで非表示中のノードは対象外。同種を重ねた場合は従来どおり新規追加。フローチャートはスイムレーン図と同じパレット構成のため同時に有効化。②ツールバーに「💾 上書き保存」＋Ctrl+S を新設＝開いている（前回保存した）ファイルへそのまま書き戻す。File System Access API のファイルハンドルを保持し、IndexedDB に docId（保存データへ埋め込む一意ID）で紐づけて記憶＝次に開いた時も保存先を覚えている。「名前をつけて保存」も対応ブラウザでは保存先を選べるダイアログになり、選んだファイルが以後の上書き先になる。書き込み前に保存先ファイルの docId とファイル名を照合し、別ファイルへの誤上書きを防ぐ。非対応ブラウザ（Firefox/Safari）・失敗時は従来のダウンロード保存へ自動フォールバック。③マインドマップのサイドバー「レイアウト」に枝の形の切替（曲線／直線）を新設。曲線＝従来（辺の中央から出て軸沿いに膨らむ）、直線＝互いの輪郭上の相手方向の点どうしを結ぶ。直線は枝が自分の角度の中に収まるため、兄弟を手でほぼ同じ方向へ動かしたときの追い越し（交差）が出ない。表示の切替なので履歴には積まない（方眼・ステップ番号と同じ扱い）。保存データに mindBranch を追加、旧ファイルは曲線。④マインドマップの配置を改修＝(a)Tabの子追加を「中心から見て外向き」に(従来は親の右へ70px固定で、親が中心より左だと中心へ戻り既存の枝の下へ潜り込んでいた)、(b)放射状の角度を葉の数ではなく箱が必要とする弧の長さで配り、半径をその深さの必要弧から決める(混み合う階層だけ外へ広がる)、(c)ツリーの列間隔・行高を実際の箱の大きさから決める(従来は列210px・行54px固定)、(d)兄弟の並び順はドラッグで動かした時点でエッジ順へ確定(整列のたびに位置から決め直すと配置モードを切り替えただけで順序が入れ替わるため)。これにより枝が無関係なノードを突き抜ける現象が解消。枝の見た目は変わらない）
- 次の小変更: v8.10（大変更なら v9.00）

## 記録の役割分担（what は git・why は開発記録）

- **コミットメッセージ**に「その版で何を変えたか（what）」を具体的に書く（版アップごとに必ずコミット）。
- **`開発記録*.txt`** は「なぜ・経緯・バグの原因と対策・既知の注意（why / how）」に絞って簡潔に書く。
  単純な変更点の羅列はコミットメッセージに委ね、二重記述を避ける。
- **`開発ロードマップ.txt`** は今後の計画を保つ。

## 運用（Git ワークフロー）

**最重要: 直接 `main` へ push しない。バージョンごとに PR → マージで履歴を残す。**
（バージョン単位のマージ履歴を残すことが、このリポジトリで GitHub を使う最大の目的。ユーザー指示 2026-06-29）

- **1 セッション ＝ 1 バージョン ＝ 1 PR。** そのセッションでバグ修正以外の変更を加えたら
  バージョンを **1つだけ** 上げ、セッション内の追加変更もすべて同じバージョン（＝同じ feature
  ブランチ）にまとめる。PR は1セッションにつき **1回だけ**。
- **PR はユーザーが明示的に指示するまで作らない。** 勝手に `gh pr create` しないこと。
  指示が来たら、その時点までの変更を1つの PR にまとめて出す。
- バグ修正のみのセッション（版番据置の「追補」）は **バージョンを上げない**。最新版ファイルを
  直接更新して追補として記録する（新しいファイルを作らない）。これも feature ブランチで作業し、
  指示時に PR にする。
- 流れ: ① feature ブランチを切る（例 `feature/<内容>` または `feature/<内容>-vX.YZ`）→
  ② 版アップ＋変更を `git commit`（what を明記・複数 commit 可）→ ③ **ユーザー指示で** PR 作成
  → ④ マージで1バージョン確定。push は feature ブランチに対してのみ行う。
- **版を上げたら**、この「現行ステータス」欄と `開発ロードマップ.txt` を更新する
  （`開発記録*.txt` は why／経緯がある場合のみ追記）。
- リモート: GitHub private `ashikagagassoumu-cmd/BusinessDiagramMaker`。
- 設計方針: 単一HTMLファイル完結／コアエンジン共通化／既存図種の後方互換を維持。
