# ビジネスダイアグラムメーカー — プロジェクト指示

ブラウザだけで動く、単一HTMLファイル完結のビジネス図作成ツール。最新版を直接更新していく運用。

## 開発継続の前に確認するファイル（全文を毎回読む必要はない）

新セッションで開発を継続する際は、まず以下の「索引＋現行ステータス」を確認すればよい。
詳細が必要なときだけ該当ファイルを開く。

- `開発記録.txt`（索引）→ 最新の変更履歴は `開発記録_v8.txt`（アクティブ）
- `開発ロードマップ.txt`（全体計画＋現行ステータス＋索引）
- `運用ルール.txt`（バージョニング・作業ルール）

## 現行ステータス

- 最新版: `diagram_v8.08.html`（2026-07-25／ガントに①折り返し表示モード、②休日にかかる工程の点線表示を追加。①時間軸を一定の区切りごとに次の段へ折り返すモード（既定OFF＝従来どおり）。段ごとに段見出し＋時間軸ヘッダー＋左のタスク名列を繰り返し、横スクロールを不要にする。折り返し単位は目盛りより1段上＝「時」→日ごと(1段24列)／「日」→週ごと(1段7列・**日曜始まり**)・月ごと(1段=その月の日数)／「週」→月ごと／「月」→年ごと(1段12列)。月ごとは「1日から月末」を1段に出す（週目盛りは列が割り切れないため列を切り上げて描き、段の内容を月末で `clipPath` により切る＝`ganttBandClipId`／バーの切り位置も `ganttBandEndX`）。**段ごとに「その段に工程がある行」だけを出し、工程が1つも無い段は段ごと省略**（フェーズ見出し行も配下メンバーが残らなければ出さない）。このため段の高さは可変で、段一覧は `ganttBands()`=[{i,dy,H,layout}] が持つ。段をまたぐバーは矢羽根で継続を表示。印刷(PDF)は「1段=1行」で改ページし、段が自前でヘッダーを持つため反復\<thead\>は使わない。実装方式＝段ごとに軸原点を段の先頭へ差し替え、Yは平行移動グループ(`ganttLayer`)でずらして既存の renderGantt* を再利用。②ツールバーの休日（土/日/祝）チェックでONの日にかかるタスクバーは、その区間だけ枠を破線＋塗りを薄くして非稼働を明示（進捗塗りも薄く入れる）。適用はタスクバーのみ・日/時モードのみ。休工＝バーを消す／休日＝破線 で使い分け。バーは rect→path 描画に変更し端の形を左右別指定（平ら/角丸/矢羽根）＝内側の継ぎ目に線を引かず1本のバーとして読める。**既知の未対応: Excel出力は折り返しに未対応**（時間軸は一直線で書き出す・出力時にヒント表示））
- v8.08 追補②（2026-07-25／版据置）: 折り返しONで「段の右端」の表示が崩れる不具合を修正。原因は右端の基準が段ではなくキャンバス全体(SVG_W)のままだった箇所＝①フェーズのロールアップバーに段のクリップが無く段の右外まで伸びていた（タスクバーのみ対応済みだった）／②時間軸ヘッダー中段の横線が突き抜け／③フェーズ行の色帯がキャンバス右端まで伸びる。「描画してよい右端」を `ganttRightEdge()`（折り返し中=段の右端＝区間の終わり／通常=SVG_W）に集約し、はみ出し文字の右詰め基準も載せ替え。切ったフェーズバーは脚を出さず矢羽根に。あわせて見出し欄ONで最小幅1120まで広がるのに段が狭く右半分が空白になる問題を、列幅を広げて段を幅いっぱいに使う方式で解消（`ganttComputeColW` の折り返し分岐・基準は段の正味の長さ `ganttWrapMaxBandSpan`＝週目盛り＋月ごとの切り上げぶんを空白にしない）。段に出ていない行への依存線も描かないように。折り返しOFFは v8.07 と完全一致（回帰なし）。
- v8.08 追補③（2026-07-25／版据置）: 左情報欄の文字が列の枠を越える不具合を修正（v8.07 から存在）。真因は `ganttClip` の文字幅見積りが一律 fontPx*0.62 で、全角が多い文字列（時モードの「工程：2026年8月12日 0時～2026年8月30日 0時」等）を短く見積もっていたこと。`ganttTextW`（全角=fontPx／半角=fontPx*0.55・見出し欄の mLab と同基準）に集約し、工程行は切って…にせず入るフォントまで落とす（`ganttFitFs`・下限9px）。あわせて折り返しONで段をまたぐバーのラベルが切る前の座標に置かれ左情報欄の上に乗る問題（実測34px侵入）を、切ったあとの座標基準に修正。フェーズ名がバーに入らず段の右にも余地が無い場合はバー内に白字で収める。実測(getBBox)で全モード・長い名称/時刻付き条件でも越境0件（同条件の v8.07 は4件・最大18.8px超）。折り返しOFFは v8.07 と完全一致。
- v8.08 追補④（2026-07-25／版据置）: 左情報欄の無記入の項目の空白を詰める。タスク行が常に「タスク名／工程／概要／担当」の4行ぶんを確保していた（`ganttRowMaxLines` が最低1を返す）ため、概要や担当を使わない運用では全タスクに空白行が入っていた。概要（行3）・担当（行4）のどちらも無記入なら行を作らない＝空のタスク行は「タスク名＋工程」の2行ぶん（両方空=54px／片方だけ入力=71px／両方入力=88px）。ON/OFF設定は作らず常時適用。「そのセルに何か描かれるか」の判定を `ganttCellShown` に集約（描画側の表示条件と高さ計算が別式でズレるのを防ぐ）。詰めたセルには当たり判定を置かないため両方空の行はインライン入力口が無くなるが、編集モーダル（タスク名・工程行・バーのダブルクリック）の情報欄から入力でき、入力すれば行が伸びて以降はインラインでも編集可。工程↔情報欄の区切り線とその下の余白13pxはセルが無い行でも残す（列の縦区切りの上端であり、消すと空の行だけ列のグリッドが途切れる）。末尾の空き行も4行→2行に追従（v5.03の「空き行＝空のタスク行と同じ高さ」を維持）。概要・担当に文字がある条件では v8.07 と完全一致。
- v8.08 追補⑤（2026-07-25／版据置）: ①折り返しON時は凡例を横一列にして**見出しの下（表題・情報行・承認欄より下）に右詰め**で配置（`ganttHeadModel` の lgInline）。折り返しONではキャンバス幅が1段ぶん（≒最小幅1120）に縮むため、右上に縦積みしていた凡例（幅392）が見出しの情報行と横に重なっていた（A3縦・A4縦の印刷でも同様）。折り返しOFFは従来の縦積みのまま。当初は「最上段＝作成日の左」に置いたが幅を作成日・表題と取り合い項目が増えると文字が縮む（6項目で11→約9px）ため、イメージ図で確認のうえこの配置に変更＝全幅が使えて10項目でも11pxのまま入る。情報行は見出し下端から上へ詰める配置なので、凡例の行を含まない高さ `baseH` をモデルに持たせて基準にした（H は凡例の行を含む全体）。入る文字サイズは「固定部＋文字幅×フォントサイズ」を線形に解いて求め（比率で概算すると固定部が縮まず収まらない）、下限7px→項目間を詰める→ラベル末尾を…で切る の順で必ず1列を保つ。②凡例のフェーズの色見本を実際のロールアップバーと同じ形（上のスパイン＋左右端が下へ伸びる脚）の path に変更＝比率は本体:脚の深さ6:11（実バー15:25と同比）、枠線も実バーと同色。角丸の帯ではタスクバーと見分けが付かなかった。折り返し(週/月/年)×承認欄ON/OFF×情報行2/10件×凡例10項目の全19通りで、凡例が表題・情報行・作成日・承認欄と重ならないことを実測確認。
- v8.07（2026-07-21／ガント休日グレーアウトを刷新。①時（1時間）モードは夜間20:00〜翌6:00を毎日薄グレー（非稼働）＝土日でも終日塗りをやめ夜間のみに。②ツールバー「休日」に土/日/祝チェックボックスを新設（表示スケールの右隣）。ONの曜日/祝日は全モードで0:00〜24:00を終日グレー。既定は土日祝すべてON。③祝日は内蔵ロジックで自動判定（固定祝日＋ハッピーマンデー＋春分/秋分＋振替休日＋国民の休日／外部取得なし）。土日祝ラベルは常時赤字。④時モードの初日を0:00起点に固定。⑤土日祝のグレーアウトでフェーズ帯の色が途切れる不具合を修正（フェーズ行の時間軸部分をグレーの前面へ再描画）。週/月モードの画面グレーアウトは列粒度が粗く従来どおり非対象。holidays 未設定の旧ファイルは土日ON・祝OFFで従来互換）
- v8.07 追補（2026-07-22／版据置）: 印刷(PDF)のページ分割を「フェーズ毎に必ず改ページ」→「基本は極力1ページにまとめ、フェーズがページ境界で途切れる時だけ改ページ」に変更（従来仕様へ戻す）。expPDF のフェーズ強制改ページ(pg-break)を廃止し page-break-inside:avoid のみに。時間軸ヘッダーの全ページ反復・表題部1ページ目のみは維持（ヘッダー欠落バグは再発しない）。
- v8.07 追補②（2026-07-22／版据置）: 印刷(PDF)の時間軸ヘッダーが2ページ目以降に出ない不具合を「実機PDF目視」で再修正。真因は2つ＝①チャンク上限がヘッダー分を差し引かず用紙を僅かに超過（"ヘッダー＋チャンク">用紙）で<tr>が用紙をまたぎ分割、②要素数の多いガントSVGを反復<thead>に直接置くと Chromium が2ページ目以降でヘッダーを描かない。対策＝①maxChunkPx を `(W/倍率)×0.60−ヘッダー高` に是正、②反復ヘッダーを高解像度PNGにラスタ化して `<img>` で置く（本体はSVGのまま／失敗時はSVGへフォールバック）。付随で expPDF を async 化しポップアップは同期先開き→生成後に差し替え。全スケール・見出し欄ON/OFFで全ページにヘッダー反復を Chromium 実PDFで確認（回帰なし）。既知: ヘッダーは画像化のため文字選択/検索不可。
- v8.06（2026-07-21）: ガント＝新スケール「時（1時間ごと）」を追加（既定は日モード）。24時間通し・複数日OK・1時間スナップ。時モードは開始/終了が日時（datetime-local・終了排他）。ヘッダー上段＝日付/下段＝時。ツールバー「時」・Excel(1時間刻み列)対応。休工は日単位（既知の制約）。
- v8.06 追補（2026-07-21／版据置）: 印刷(PDF)出力で日付・時間の時間軸ヘッダーが2ページ目以降に出ない不具合を修正（時/日/週/月すべて）。本体を「フェーズのまとまり／1タスク=1行」の <tr> に分割し改ページを行境界に落として <thead>(時間軸＋列見出し)を全ページ先頭で再描画。フェーズの切れ目は必ず改ページ・表題部(工事名等)は1ページ目のみ。
- v8.05 追補（2026-07-14／版据置）: ①ガント＝フェーズの括りを変えて所属フェーズが変わったタスクの色を自動追従（無所属は既定色へ）。②樹状図＝編集ダイアログの空行が保存時に消える不整合を修正（行も列と同じく空欄で保存可。「ラベル有→通常ノード上書き後に行が増やせない」の原因）。③マインドマップ補足情報（付箋）＝枠線・文字・接続線は属する枝色、塗りは淡い枝色に。④マインドマップ＝複数選択したシェイプをまとめて移動（選択保持・付け替えは単独時のみ）。⑤マインドマップ＝重要度によるサイズ差を三段階に拡大（高/中/低、重要度未設定は基準サイズ据置）。
- 次の小変更: v8.09（大変更なら v9.00）

## 記録の役割分担（what は git・why は開発記録）

- **コミットメッセージ**に「その版で何を変えたか（what）」を具体的に書く（版アップごとに必ずコミット）。
- **`開発記録*.txt`** は「なぜ・経緯・バグの原因と対策・既知の注意（why / how）」に絞って簡潔に書く。
  単純な変更点の羅列はコミットメッセージに委ね、二重記述を避ける。
- **`開発ロードマップ.txt`** は今後の計画を保つ。

## 運用（Git ワークフロー）

**最重要: 直接 `main` へ push しない。バージョンごとに PR → マージで履歴を残す。**
（バージョン単位のマージ履歴を残すことが、このリポジトリで GitHub を使う最大の目的。ユーザー指示 2026-06-29）

- **1 セッション ＝ 1 バージョン ＝ 1 PR。** そのセッションでバグ修正以外の変更を加えたら
  バージョンを **1つだけ** 上げ、セッション内の追加変更もすべて同じバージョン（＝同じ feature
  ブランチ）にまとめる。PR は1セッションにつき **1回だけ**。
- **PR はユーザーが明示的に指示するまで作らない。** 勝手に `gh pr create` しないこと。
  指示が来たら、その時点までの変更を1つの PR にまとめて出す。
- バグ修正のみのセッション（版番据置の「追補」）は **バージョンを上げない**。最新版ファイルを
  直接更新して追補として記録する（新しいファイルを作らない）。これも feature ブランチで作業し、
  指示時に PR にする。
- 流れ: ① feature ブランチを切る（例 `feature/<内容>` または `feature/<内容>-vX.YZ`）→
  ② 版アップ＋変更を `git commit`（what を明記・複数 commit 可）→ ③ **ユーザー指示で** PR 作成
  → ④ マージで1バージョン確定。push は feature ブランチに対してのみ行う。
- **版を上げたら**、この「現行ステータス」欄と `開発ロードマップ.txt` を更新する
  （`開発記録*.txt` は why／経緯がある場合のみ追記）。
- リモート: GitHub private `ashikagagassoumu-cmd/BusinessDiagramMaker`。
- 設計方針: 単一HTMLファイル完結／コアエンジン共通化／既存図種の後方互換を維持。
