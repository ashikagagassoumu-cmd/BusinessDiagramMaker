# ビジネスダイアグラムメーカー — プロジェクト指示

ブラウザだけで動く、単一HTMLファイル完結のビジネス図作成ツール。最新版を直接更新していく運用。

## 開発継続の前に確認するファイル（全文を毎回読む必要はない）

新セッションで開発を継続する際は、まず以下の「索引＋現行ステータス」を確認すればよい。
詳細が必要なときだけ該当ファイルを開く。

- `開発記録.txt`（索引）→ 最新の変更履歴は `開発記録_v8.txt`（アクティブ）
- `開発ロードマップ.txt`（全体計画＋現行ステータス＋索引）
- `運用ルール.txt`（バージョニング・作業ルール）

## 現行ステータス

- 最新版: `diagram_v8.15.html`（2026-08-18／シェイプ拡充＋重ね置き完成＋接続線美観）
  - **「システム入力」シェイプ新設**: `sys_input`（長方形＋両端の二重線・定義済み処理の記法）と
    `sys_input_desc`（説明有＝note と同じタイトル＋備考欄に二重線）。スイムレーン／フローチャート／BPMN の
    3図種に追加。挙動は rect／note と同等（レーン所属・クランプ・障害物・吸着・番号バッジ・配色テーマ・
    デザインテンプレ・重ねドロップ置換・AUTO_FIT_SKIP）。二重線は文字の流れと直交する辺（縦モード=左右／
    横モード=上下）。文字余白は `autoFitPad` が+12px。色はデータの藍 #e0e7ff/#6366f1 を引き継ぎ
  - **データ＝書類と同列化**: `isOverlayShape` に parallelogram を追加（1関数で経路障害物除外・吸着無効・
    クランプ免除が全部そろう設計）。BPMNパレットにも「データ」を追加して3図種でそろえた
  - **書類・データをレーン境界に被せられない不具合を修正**: レーン内へ収める座標クランプ6系統（配置
    addNode／ドラッグ縦横／矢印キー nudgeSel／レーン幅変更／文字量変化 clampNodeIntoBounds／シェイプ置換）
    の除外条件に isOverlayShape が漏れていた。**所属レーンの更新（lid）と追従は残したままクランプだけ免除**
  - **レーン境界の当たり判定を最下位に**: 境界のドラッグ帯が最前面（g-dividers）にあり、v8.13⑤の
    穴あけ（laneBandGaps/_edgeHitGeom）は線とつまみだけが対象＝境界に被った接続線ラベルの○アイコンが
    ダブルクリックできず、経路編集の選択も帯に奪われていた。帯を g-edges より下の専用レイヤ
    `g-lane-bands` へ移し「境界は他の何よりも優先度が低い」を層の順序で保証（穴あけ機構は廃止）。
    凡例・組織図ボタン・再接続ハンドルは従来どおり最前面 g-dividers
  - **接続線のZ字経路を隙間の中央で曲げる**: 同コストのZ字はA*が探索順の都合で終点際に張り付いた形を返し、
    水平線が相手の箱の真上を這っていた（参考資料「接続線経路サンプル②.pdf」パターン①）。旧ルーター
    calcMidY の美観規則を orthoRoute の後処理 `centerZ` として復元＝曲がり2回・出口入口が同軸対向の前進
    接続だけを対象に、交差セグメントを (sy+ey)/2 へ平行移動（clr() で塞がれば前後にずらし、ダメなら現状
    維持）。L字・直線・手動経路・クランク・3回以上曲がる迂回は不変
- 旧: `diagram_v8.14.html`（2026-08-18／UX改善13件）
  - **接続線の1回曲げ(L字)を実効化**: v8.13追補②のポート割当は働いていたが、経路を作る `orthoRoute` の
    A* が「始点の進行方向」を持たず、同コストの角を近傍の列挙順（横が先）だけで選ぶため、縦モードでは
    必ず逆側の角になって3回曲げの階段に化けていた（修正前のZ字2回曲げより悪化）。出入りの軸が直交し
    2本ともどの箱も通らないなら A* を回さず即L字を返す近道を入れ、`pdir[src]` を出口の辺で初期化。
    `lShapeClear` の障害物条件も orthoRoute とそろえた（余白12→9px・始終点を内包する箱を除外）
  - **文字サイズ**: ノードごとに `n.fontSize`。`nodeFontSize`/`lineH`/`vch`/`vcol` を新設し行送り・列間隔を
    比例化（既定12pxでは従来の15/18/17と同値＝既存図は不変）。UIは「色」の右に Word/Excel と同じ
    〈数値欄＋▼一覧〉＋〈A˄拡大／A˅縮小〉。12に戻すとキーごと削除
  - **文字の自動縮小**: 手で小さくした箱では `fitNodeFontSize` が幅に収まる最大サイズまで縮める（下限8px）。
    ただし**自分で入れた改行のぶんは必ず場所を確保**＝改行したら高さが伸びる。書類は波型(10px)を高さに加算
  - **改行時の中心ずれ**: ①説明有プロセスの備考は確定時に高さを直接代入していて render 側の中心固定が
    素通りしていた→計算を render 側に一本化 ②`AUTO_W_MAX`(260)>レーン幅(220)で必ずクランプが起き、
    壊れた中心が次の基準になって戻らなかった→自動拡張の上限をレーン幅までにし、超える箱は端寄せでなく
    レーン中央そろえ ③縦書きの列ごと独立センタリングを列頭そろえに
  - **編集中プレビューの一致**: 編集欄の組版（文字サイズ・行送り・折り返し幅・word-break）を確定後の描画に
    そろえた。Alt+Enter は value 直接書換で input が出ず箱が追従しなかったので自分で通知
  - **文字揃え**: `_editTarget` を新設し「編集中はその欄だけ／シェイプ選択中は全体」に分離。揃えボタンに
    `preventDefault` を付け編集を閉じないようにした。備考は `noteAlign`、区画は `sections[i].align`
  - **Ctrl+ドラッグのコピー**: `revertDragToCopy` を新設し keydown/keyup で双方向に。押している間だけコピー
  - **複数レーンプロセス**: カーソルを move に統一、`n.thick` で厚みを変えられる帯を追加。レーンの無い
    図種に置いたものは帯を出さず8方向ハンドル（掴むと x=0 へ飛ぶ不具合も解消）
  - **書類・データのレーン連動**: `isFreeShape` から doc/parallelogram を外し、`nodeLaneIdx` で位置からも
    レーンを判定（旧ファイルも追従）。レーン幅変更・並べ替えで一緒に動く
  - **旧ファイルのインポート**: 保存データに `appVer` を持たせ、旧版なら経路指定（waypoints/orthoWps/
    midPos/fromPin/toPin）を落として今の仕組みで引き直す。**引き継ぐのは配置・編集内容だけ**という方針。
    併発していた `style`/`stepNums` の復元漏れも修正
  - **デザイン・ステップをサイドバーへ**: ガント・マインドマップでは効かないためメニューバーから撤去し、
    効く図種のときだけ左サイドバーに表示。見本にシェブロン・ピルの形状差も反映
  - **接続線ラベル**: 140pxで折り返し、欄の大きさを行数・列数に追随。編集欄も同じ組版で背が伸びる
  - **保存/読込ダイアログ**: 3か所とも `id:'bdm-save'` 済みだったのに加え、開いているファイルの場所を
    `startIn` で渡す保険を追加（非対応時は指定なしで開き直す）
- v8.14追補（2026-08-18・版番据置）: ①フローチャートの整列スナップ許容差を 6px→3px（`alignTol()` に図種分岐）
  ②書類(doc)を「重ね置きシェイプ」に＝`isOverlayShape` 新設。経路探索の障害物3か所（routeObs／orthoRoute／
  lShapeClear）から除外し、配置・ドラッグの吸着（整列ガイド・レーン内1/4等・主軸・方眼）も全て無効化。
  レーンへの所属と追従（v8.14本編）はそのまま＝「レーンには属すが位置は自由・線は避けない」
  ③手動編集した経路（orthoWps/waypoints）がシェイプの移動に追従せず、置き去り→斜め判定→自動経路へ破棄
  されていた件を修正＝動かしたシェイプに接する線の曲がり角・支点を同じ実移動量（スナップ・クランプ後）だけ
  平行移動。ドラッグ中は表示のみずらし mouseup の `bakeManualRouteShift()` で一度だけ実データへ焼き込む
  （コピー複製との干渉回避）。矢印キー移動（nudgeSel）も同様
  ④**交差ブリッジの全面是正**＝「交差しているのに山が出ない」20条件を全数調査し5系統を修正:
  (1)端点共有ペアの全面除外→「共有シェイプの接続矩形+20px内」だけ除外（離れた本物の交差には山）
  (2)収まらない山は半径12→最小6pxに縮小・中心シフト・密集クラスタは最大ギャップ分割（u空間計算＋
  prevEnd で弧の単調性保証。角丸ゾーン内のみ諦め＝残存限界）(3)手動支点の曲線は「直交線側が常に跨ぐ」
  形で対象化（ベジエ8分割の折れ線近似 sampleSmoothPts/bridgeCollectVsPoly）(4)自己交差も後の区間側に山
  (5)200本上限の母数を対象本数に・告知フラグを本数回復でリセット。収集はペア別→エッジ別蓄積→正規化1回
  （findCrossingsRaw）。T字接触・斜め・平行・バス・ドラッグ中省略・別レンダラ図種は不変
- 旧: `diagram_v8.13.html`（2026-08-17／スイムレーン等レーン図の印刷・画像書き出しで、伸長した編集領域ぶんの空白を切り詰め＝印刷範囲を内容に最適化。スイムレーン／BPMN などレーンを使う図種は、編集のドラッグ余地を確保するため主軸（縦レーン＝高さ SVG_H／横レーン＝幅 SVG_W）が calcCanvasSize で「内容の端＋200px・グリッド切り上げ・最小800px」まで自動で伸びる。従来この余白ぶんがそのまま印刷（expPDF）・PNG・SVG に載っていた（非レーン図は v7.09 で contentBBox に合わせて切り詰め済みだったが、レーン図は !usesLanes() 条件で対象外だった）。対策＝書き出し時に主軸（伸びる方向）だけを実際の内容までに切り詰める共通ヘルパ laneExportMainExtent() を新設し、buildExportSvg（PNG/SVG）と expPDF（印刷）の両方で使う。交差軸（レーン幅の合計）と見出し帯は全レーン残す＝レーンは構造なので落とさない。集計は calcCanvasSize と同じく全ノード（crosslane＝複数レーンプロセスを含む）の主軸端の最大値＋小さめの余白で、末尾に crosslane を置いても切れない。内容が無ければ null を返し従来どおり全キャンバスを写す。縦横（orientation）両対応。常時オン（トグルなし）。レーン背景・区切り線は全キャンバスに描いたまま viewBox で切り取るので空白は消えトリム領域はレーン色で埋まる。レーン外枠の閉じ線（縦＝下端 y=SVG_H／横＝右端 x=SVG_W）は元の全キャンバス位置に描かれ viewBox 外になるため、buildExportSvg でトリム位置（_ext のわずか内側）に外枠と同色（#94a3b8・1.5px）の閉じ線を1本描き足して枠を4辺そろえる＝PNG・SVG・印刷すべてに反映（buildExportSvg 1か所）。非レーン図・ガントの印刷挙動は不変）
- v8.13追補（2026-08-17・版番据置）: 接続線まわりの是正が中心。①交差ブリッジ（半円の飛び越え）を作り直した＝
  従来は「戻り線 × 順方向線」の一方向しか見ておらず、順方向どうし・戻り方向どうしの交差には山が出ないため
  「交差が何か所あっても1か所しかブリッジされない」状態だった。全ペア総当たりにし、ペアごとに
  「戻り線が跨ぐ／同種なら S.edges で後にある線が跨ぐ」で必ず一方だけを跨ぐ側に決める。あわせて、
  交差ありの線の経路"全体"に白い縁取りを敷く処理（並走する線やレーン色まで白く削れ、検出できなかった交差が
  「ただ途切れた線」に見えていた原因）を廃止し、renderEdges 末尾の第3パスで「山ごとに 白5px → 本体色」を
  最前面に描き直す＝跨がれる側は半円と交わる位置だけが途切れ、交差していない場所は切れない。
  判定も収集(bridgeCollect)＋正規化(bridgeNormalize)の2段に作り替え、同一点の除去・近接交差のひと山への統合・
  「その角で実際に使われる角丸半径」を端部マージンにすることで、角への食い込み・二重挿入・逆順を構造的に無くした
  （半径は wpPath と同じ18に統一。横の線も上へ膨らませる＝定番の飛び越え記法）。②別レーンへの接続を1回曲げに＝
  v8.02 の「入口を主軸の辺へ強制」は出口も主軸になるため必ず2回曲がる（Z字）。交差軸で24px以上離れていて、かつ
  L字の2本が他の箱を貫通しない（lShapeClear が routeObs/vHit/hHit で確認）ときだけ「出口＝主軸の辺／入口＝源の
  ある側の横辺」にする＝参考資料/購買申請業務 As-Is の形。貫通する場合と真下・真上は従来どおり。③同じ辺に集まる接続点を
  「等間隔・最小10px」にそろえた（②で横辺へ入る線が増え、そこから水平に出る線と重なるため）。等間隔にしても10pxを
  割る箱は growForPorts が 10×(本数+1) まで自動で広げる（中心は動かない）。3rd pass は既に10px以上離れている辺には
  触らず（一直線優先・段違いトラックを維持）、詰まっている辺だけを等間隔へ戻し、動かした線は相手側が1本なら
  一直線に整え直す。ポートで直せない無関係な線どうしの共線重なりは separateOverlaps が経路側で退避させる。④自動経路がレーン境界の真上に乗らないように
  した（orthoRoute 内 avoidLaneLines。境界から10px未満の"内部の"区間だけを退避。端点に接する区間は動かさず、
  ノードを貫通するなら元のまま）。⑤レーン境界のドラッグ帯（最前面 g-dividers・幅8px×全長）に穴をあけ、線そのものと
  経路編集つまみを境界より優先させた（レイヤ順は変えない＝線をノードより前面に出すとノードのクリックを奪うため。
  renderEdges が描いた実座標を _edgeHitGeom に控え、renderDividers が範囲を抜いた複数 rect を置く）。
  ⑥文字が2行以上になってもシェイプの中心が動かないようにした（setSizeKeepCenter を新設し autoFitNodeSize／
  calcMindNodeSize／note の備考量調整／sections の4か所を通す。大きさが変わらないときは何もしない＝再描画で
  座標が流れない。レーンからはみ出す場合だけ枠内優先で中心がずれる）。⑦キャンバスのレーン見出しをダブルクリックで
  改名できるようにした（サイドバーと併存。編集欄は openSvgEditor の opts.host で重ね箱 #gstick-lane の中に置く＝
  #diagram-svg 直下だと見出し帯の裏に隠れるため。固定表示中のズレ補正も不要になる。見出しの単純クリックは
  220ms の遅延 render にして dblclick を成立させる。確定で snap＋lanePanel＝Undo とサイドバー同期が効く）。
  ⑧印刷（PDF出力）で上の枠線だけが細い／消える不具合を修正（図面名の白い帯が最前面で枠線の上半分を塗っていた→
  最背面へ。あわせて viewBox を四辺1px広げて左端・下端の線の欠けも解消。ガントの分割 viewBox は不変）。
  ⑨書き出し・印刷に配置ガイド（黄色い点線）等が写り込まないようにした（buildExportSvg の clone から
  g-guides／g-replace-hl／#prev-edge を落とす。画面側の状態には触らない）。⑩いちばん下のシェイプを画面中央まで
  送れるようにした（#zoom-spacer にだけ「見えている大きさの半分」を足す。SVG_H は変えないので印刷・書き出しに
  空白は増えない。ガントは対象外）。
- v8.11（2026-08-06／ガント工程表の見出し欄・時間軸ヘッダー・タスク名列をスクロールに追従して固定＝表計算のウィンドウ枠固定と同じ振る舞い。縦にスクロールしても見出し欄（工事名・承認欄・凡例）と時間軸ヘッダーは画面上端に残り、横にスクロールしてもタスク名列（＋フェーズ列）は左端に残る。その交点の左上コーナー（「フェーズ」「タスク / 概要 / 担当」の見出し・列幅ハンドル・⊕列追加）は縦横どちらでも動かさない。固定はブラウザの position:sticky に任せる＝スクロール中に走る JS が無いので完全に止まって見える（当初は v8.10 のレーン見出しと同じくスクロール量ぶん JS で平行移動していたが、スクロールの描画に1フレーム遅れて追いつくため見出しがわずかに揺れた）。本体SVGの前に幅0・高さ0の重ね箱（#gstick-left／#gstick-top／#gstick-corner）を置き、その中のSVGへ専用レイヤ（g-gantt-left／g-gantt-top／g-gantt-corner）を描く。固定したい軸だけ top / left を指定し、指定しない軸は本体と一緒に流れる。position:sticky は SVG の中身には効かないため本体SVGの外に出しているが、座標系は本体と同じなので当たり判定（svgPt は本体SVGの行列で換算）はそのまま流用でき、書き出し時は buildExportSvg が3レイヤを取り込む。図の座標は動かさないので、保存・PDF・画像・Excel 出力は図の本来の位置で出る。あわせて v8.10 のレーン見出し（スイムレーン／BPMN ほかレーンを使う図種）も同じ作りへ寄せ、同じ揺れを解消した＝重ね箱 #gstick-lane に載せ、止める軸を向きで切り替える（縦モード＝上端／横モード＝左端）。レーン並べ替えは交差軸だけを見るため座標補正は不要で、スクロールしたまま掴んでも従来どおり動く。／⑤メニューバーの再表示ボタンを画面右下から見出しバー（隠したあと唯一残る帯）の右端へ移した＝メニューが畳まれた先が一目で分かる。／⑥ガントの見出し欄に「▲ 見出しを隠す」を新設（見出し欄の右下）。畳んでも工事名だけは残り、その帯の右端に「☰ 見出しを表示」が出る。畳むと情報行・承認欄・凡例が消えて見出しは62pxまで縮み、そのぶん工程が上へ詰まる。開閉ボタンはSVGではなくHTMLの重ねボタン＝見出し欄はキャンバス幅いっぱいでSVGに描くと「右端」が画面外になること、PDF・画像の書き出しに操作ボタンを載せないことの2点が理由。ボタンも見出しと同じ position:sticky の重ね箱に入れてあるので、固定時は画面上端に留まり折り返し表示中は見出しと一緒に流れる。状態は S.gantt.header.collapsed に持ち保存されるが、表示の切替なので履歴には積まない。／⑦ハンドツール中はホイールを拡大縮小に割り当てた（掴んで動かすモードでは画面移動はドラッグで行うため）。ズームの中身は図種ごとの既存処理に委ねる＝ガントは列幅・他は zoomLevel。固定した帯の下には本体の行やバーが潜り込むため、各レイヤの最下部に透明のシールドを敷いて「見出しをクリックしたのに下のバーを掴む」誤操作を防ぎ、あわせて掴んだ位置を見えているものの座標へ読み替える（ganttStickyPt）＝情報欄クリックでの工程追加は横スクロール後も従来どおり効く。行ドラッグは縦位置・列幅ドラッグは横の移動量で判定するため座標補正は不要。見出し欄は縦のみ固定＝横は流す（キャンバス幅いっぱいに広がるため両方向で固定すると右端の承認欄・凡例に永久に届かなくなる）。折り返し表示中は段ごとに時間軸ヘッダーを持ち横スクロールも起きないため対象外）
- v8.11追補（2026-08-06・版番据置）: ①未保存の変更を検知できるようにした＝保存する内容そのものを文字列化して
  最後に保存した時点と比べる「指紋方式」（docState/docFingerprint/markSaved/isDirty）。snap() の履歴は
  lanes/nodes/edges/gantt/mindLayout の5キーしか積まないため、方眼のオン・オフやデザインテンプレート、図の名称、
  レーン名などは履歴に載らず取りこぼす。uid（undo で巻き戻らない）と docId（読み込み直後に遅延生成）は指紋から除く。
  ②未保存のまま今の図を捨てる操作（図種切替・新規作成・ファイル読み込み）の前に4択ダイアログ confirmSave4 を出す
  ＝上書き保存／名前を付けて保存／保存しない／キャンセル。保存を選んだ場合は保存が成立したときだけ次へ進む。
  ③タブを閉じるときは beforeunload で止めるが、そこに出るダイアログの文言もボタンもブラウザが決める固定文なので、
  キャンセルして残ってくれた直後にこちらの3択（保存しないに当たる動作が無いため）を出す。④保存の結果を画面中央の
  トースト #toast で出す＝成功2秒・失敗4.2秒＋原因を日本語化（SAVE_ERR_JA/saveErrText）。#hint は従来どおり残す。
  保存ピッカー・読み込みピッカーの中止（従来は完全に無言）にも hint を出す。⑤マインドマップに「リザーブ領域」を新設
  ＝画面右のサイドバーに入力欄と一覧を置き、置き場所が決まらない話を貯めておく。一覧の各行は配置後と同じシェイプ
  そのものを描き（問題=角丸／対策=矢印タグ／補足情報=付箋）、右に極小の分類ボタン3つを縦積み。ダブルクリックで
  テキスト編集、ドラッグで図へ配置＝空き場所なら選択中ノード（無ければ中心）の子、既存トピックの上ならその子。
  v8.09 の「重ね置きで形状だけ入れ替え」は通さない（入れ替えると入力した文章が失われるため）。
- v8.12（2026-08-06／「図の名称」の入力欄を左サイドバー最上段の `#app-header` から
  見出しバー `#app-title-bar` 内・図種バッジ `#dtype-label` の直後へ移設した。旧位置は幅166px・12px・ラベル無しで
  白いパネルの中の白い枠＝どこに入力するのか分からないという指摘があった。濃紺のバーの中で白塗り＋14px
  （図種バッジ12pxより少し大きい）にして、入力できる場所だと一目で分かるようにしている。input は親の font を
  継承しないため、バーの font-size:18px / font-weight:800 / letter-spacing:.5px は input 側で明示的に打ち消す。
  `min-width:0` を付け、狭い画面では280pxから縮んで他要素を押し出さないようにした。`aria-label` も追加（従来は
  placeholder だけでアクセシブルな名前が無かった）。JS は `#diagram-name` を getElementById で引いており親要素に
  依存しないため変更なし。空になった `#app-header` と、元から対応要素の無いデッドCSS `#app-title` は削除した。
  あわせて操作マニュアルを v8.12 として全面改訂＝v8.00 版から11版ぶん未反映だったものを反映し、スクリーンショットも
  44点すべて撮り直した）
- 次の小変更: v8.16（大変更なら v9.00）

## 記録の役割分担（what は git・why は開発記録）

- **コミットメッセージ**に「その版で何を変えたか（what）」を具体的に書く（版アップごとに必ずコミット）。
- **`開発記録*.txt`** は「なぜ・経緯・バグの原因と対策・既知の注意（why / how）」に絞って簡潔に書く。
  単純な変更点の羅列はコミットメッセージに委ね、二重記述を避ける。
- **`開発ロードマップ.txt`** は今後の計画を保つ。

## 運用（Git ワークフロー）

**最重要: 直接 `main` へ push しない。バージョンごとに PR → マージで履歴を残す。**
（バージョン単位のマージ履歴を残すことが、このリポジトリで GitHub を使う最大の目的。ユーザー指示 2026-06-29）

- **1 セッション ＝ 1 バージョン ＝ 1 PR。** そのセッションでバグ修正以外の変更を加えたら
  バージョンを **1つだけ** 上げ、セッション内の追加変更もすべて同じバージョン（＝同じ feature
  ブランチ）にまとめる。PR は1セッションにつき **1回だけ**。
- **PR はユーザーが明示的に指示するまで作らない。** 勝手に `gh pr create` しないこと。
  指示が来たら、その時点までの変更を1つの PR にまとめて出す。
- バグ修正のみのセッション（版番据置の「追補」）は **バージョンを上げない**。最新版ファイルを
  直接更新して追補として記録する（新しいファイルを作らない）。これも feature ブランチで作業し、
  指示時に PR にする。
- 流れ: ① feature ブランチを切る（例 `feature/<内容>` または `feature/<内容>-vX.YZ`）→
  ② 版アップ＋変更を `git commit`（what を明記・複数 commit 可）→ ③ **ユーザー指示で** PR 作成
  → ④ マージで1バージョン確定。push は feature ブランチに対してのみ行う。
- **版を上げたら**、この「現行ステータス」欄と `開発ロードマップ.txt` を更新する
  （`開発記録*.txt` は why／経緯がある場合のみ追記）。
- **版を上げたら、マニュアル生成スクリプトの版参照も追随させる**＝`capture-puppeteer.js` の `URL`
  （旧版は `99_旧バージョン/` へ退避されるため、放置すると撮影が404で落ちる）と `create-manual.js` の
  `APP_VERSION`。マニュアル本体（.docx）は `.gitignore` 対象で git に履歴が残らず、更新漏れに気づけない。
  再生成は `node serve.js` → `node capture-puppeteer.js` → `node create-manual.js` の順。
  （v8.00 版のあと11版ぶん放置され、撮影スクリプトも404のままだった反省。2026-08-06）
- リモート: GitHub **public** `ashikagagassoumu-cmd/BusinessDiagramMaker`。
  ※ 以前は「private」と記載していたが、実際の可視性は public だった（`gh repo view` で確認・2026-08-17）。
  公開のまま運用する方針をユーザーが確認済み。push＝インターネット公開なので、社内固有の情報
  （実案件の図・スクリーンショット・PDF）はコミットしない。参考資料の PDF と `screenshots/` は
  `.gitignore` で除外済み。
- 設計方針: 単一HTMLファイル完結／コアエンジン共通化／既存図種の後方互換を維持。
